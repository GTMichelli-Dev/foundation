@model Foundation.Web.Models.AppSetup
@{
    ViewData["Title"] = "Setup";
}

<h3 class="mb-2">Setup</h3>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success alert-dismissible fade show py-1 mb-2" role="alert">
        @TempData["Message"]
        <button type="button" class="btn-close btn-sm" data-bs-dismiss="alert"></button>
    </div>
}

<!-- Auto-save status (Setup saves on every change; no Save button) -->
<div id="saveStatus" class="position-fixed bottom-0 end-0 me-3 mb-3 px-3 py-1 rounded small shadow-sm"
     style="display:none; z-index:1080; background: var(--bw-surface-alt); color: var(--bw-text);"></div>

<form asp-action="Index" method="post" enctype="multipart/form-data" id="setupForm">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />
    <input type="hidden" name="activeTab" id="activeTabField" value="@(TempData["ActiveTab"] ?? "tabCompany")" />

    <ul class="nav nav-tabs" id="setupTabs" role="tablist">
        <li class="nav-item">
            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#tabCompany" type="button">Company &amp; Ticket</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabSystem" type="button">System</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabFields" type="button">Fields</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabLocations" type="button">Locations</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabEmail" type="button">Email</button>
        </li>
        <li class="nav-item" id="tabKioskPromptsNav" style="display: @(Model.KioskCount > 0 ? "block" : "none");">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabKioskPrompts" type="button">Kiosk Prompts</button>
        </li>
        <li class="nav-item" id="tabSecurityNav" style="display: @(Model.UseLogin ? "block" : "none");">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabSecurity" type="button">Users &amp; Security</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabDesigners" type="button">Ticket Designers</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabApi" type="button">API</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabServices" type="button">Services</button>
        </li>
        @if (ViewBag.ShowResetDatabase == true)
        {
            <li class="nav-item">
                <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabDatabase" type="button">Database</button>
            </li>
        }
    </ul>

    <div class="tab-content mt-3">
        <!-- TAB: Company & Ticket -->
        <div class="tab-pane fade show active" id="tabCompany">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Company Info (Ticket Headers)</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="mb-2">
                                <label asp-for="Header1" class="form-label mb-0 small"></label>
                                <input asp-for="Header1" class="form-control form-control-sm" />
                            </div>
                            <div class="mb-2">
                                <label asp-for="Header2" class="form-label mb-0 small"></label>
                                <input asp-for="Header2" class="form-control form-control-sm" />
                            </div>
                            <div class="mb-2">
                                <label asp-for="Header3" class="form-label mb-0 small"></label>
                                <input asp-for="Header3" class="form-control form-control-sm" />
                            </div>
                            <div class="mb-2">
                                <label asp-for="Header4" class="form-label mb-0 small"></label>
                                <input asp-for="Header4" class="form-control form-control-sm" />
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Ticket Settings</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-12">
                                    <label asp-for="TicketNumber" class="form-label mb-0 small"></label>
                                    <input asp-for="TicketNumber" class="form-control form-control-sm" type="number" min="@Foundation.Web.Services.TicketNumbers.Minimum" />
                                    <div class="form-text small">Tickets start at @Foundation.Web.Services.TicketNumbers.Minimum; card numbers use the numbers below it.</div>
                                </div>
                                @* TicketsPerPage hidden from the UI but still posted so the existing value persists. *@
                                <input asp-for="TicketsPerPage" type="hidden" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: System -->
        <div class="tab-pane fade" id="tabSystem">
            <div class="row g-3">
                <div class="col-md-6">
                    <!-- System Settings -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>System</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="DemoMode" class="form-label mb-0 small"></label>
                                    <select asp-for="DemoMode" class="form-select form-select-sm">
                                        <option value="false">Normal</option>
                                        <option value="true">Demo</option>
                                    </select>
                                </div>
                                <div class="col-6">
                                    <label asp-for="RecallLastValues" class="form-label mb-0 small"></label>
                                    <select asp-for="RecallLastValues" class="form-select form-select-sm">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="UseLogin" class="form-label mb-0 small"></label>
                                    <select asp-for="UseLogin" class="form-select form-select-sm" id="UseLogin">
                                        <option value="false">No Login Required</option>
                                        <option value="true">Require Login</option>
                                    </select>
                                </div>
                                <div class="col-6" id="kioskCodeSection" style="display: @(Model.UseLogin ? "block" : "none");">
                                    <label asp-for="KioskCode" class="form-label mb-0 small"></label>
                                    <input asp-for="KioskCode" class="form-control form-control-sm" maxlength="20" />
                                </div>
                            </div>
                            <div class="mb-2">
                                <div class="form-check">
                                    <input asp-for="EnableSpanish" class="form-check-input" type="checkbox" />
                                    <label asp-for="EnableSpanish" class="form-check-label small"></label>
                                </div>
                                <div class="form-text small">
                                    Off: every screen is English and no language button appears anywhere —
                                    exactly how the app behaved before Spanish was added. On: the kiosk, the
                                    phone page (/Mobile), the signature pad and the ticket views can run in
                                    Spanish. Office and admin pages are English either way.
                                </div>
                            </div>
                            <div class="mb-2" id="languageSection" style="display: @(Model.EnableSpanish ? "block" : "none");">
                                <label asp-for="Language" class="form-label mb-0 small"></label>
                                <select asp-for="Language" class="form-select form-select-sm">
                                    <option value="en">English</option>
                                    <option value="es">Español (Spanish)</option>
                                </select>
                                <div class="form-text small">
                                    What those screens show before anyone chooses. A device can override it
                                    with the EN/ES button, or with <code>?lang=es</code> in its URL — so one
                                    kiosk can run in Spanish while the rest of the site stays English.
                                </div>
                            </div>
                            <div class="mb-2">
                                <label asp-for="Theme" class="form-label mb-0 small"></label>
                                <select asp-for="Theme" class="form-select form-select-sm">
                                    <option value="default">Default (Blue)</option>
                                    <option value="dark">Dark Mode</option>
                                    <option value="earth">Earth (Brown, Black &amp; Green)</option>
                                </select>
                            </div>
                            <div class="mb-2">
                                <label asp-for="TimeZoneId" class="form-label mb-0 small"></label>
                                @{
                                    var commonTimeZones = new (string Id, string Label)[]
                                    {
                                        ("America/New_York",    "Eastern (New York)"),
                                        ("America/Chicago",     "Central (Chicago)"),
                                        ("America/Denver",      "Mountain (Denver)"),
                                        ("America/Phoenix",     "Mountain — Arizona (no DST)"),
                                        ("America/Los_Angeles", "Pacific (Los Angeles)"),
                                        ("America/Anchorage",   "Alaska (Anchorage)"),
                                        ("Pacific/Honolulu",    "Hawaii (Honolulu)"),
                                        ("America/Halifax",     "Atlantic (Halifax)"),
                                        ("America/Toronto",     "Eastern — Canada (Toronto)"),
                                        ("America/Mexico_City", "Central — Mexico (Mexico City)"),
                                        ("UTC",                 "UTC (Coordinated Universal Time)"),
                                        ("Europe/London",       "London (UK)"),
                                        ("Europe/Berlin",       "Berlin / Paris / Madrid"),
                                        ("Australia/Sydney",    "Sydney"),
                                    };
                                    var current = string.IsNullOrWhiteSpace(Model.TimeZoneId) ? "America/Chicago" : Model.TimeZoneId;
                                    var inList = commonTimeZones.Any(t => t.Id == current);
                                }
                                <select asp-for="TimeZoneId" class="form-select form-select-sm">
                                    @foreach (var tz in commonTimeZones)
                                    {
                                        <option value="@tz.Id" selected="@(current == tz.Id ? "selected" : null)">@tz.Label</option>
                                    }
                                    @if (!inList)
                                    {
                                        <option value="@current" selected="selected">@current (custom)</option>
                                    }
                                </select>
                                <div class="form-text small">Used for date pickers, completed-tickets grid, ticket prints. Pick a custom IANA ID by editing the database directly if your zone isn't listed.</div>
                            </div>
                        </div>
                    </div>

                    <!-- Icon -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Icon</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="d-flex align-items-center gap-2 mb-1">
                                <img id="iconPreview" src="/api/setup/icon" alt="Current Icon" style="height: 36px; width: 36px;" />
                                @if (Model.Icon != null)
                                {
                                    <label class="form-check-label small">
                                        <input type="checkbox" name="removeIcon" id="removeIconCb" value="true" class="form-check-input" />
                                        Remove
                                    </label>
                                }
                                else
                                {
                                    <span class="text-muted small">Default</span>
                                }
                            </div>
                            <input type="file" name="iconFile" id="iconFileInput" class="form-control form-control-sm" accept="image/*" />
                        </div>
                    </div>

                    <!-- Printing -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Printing</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="RemotePrintMode" class="form-label mb-0 small"></label>
                                    <select asp-for="RemotePrintMode" class="form-select form-select-sm" id="RemotePrintMode">
                                        <option value="None">No Remote Printing</option>
                                        <option value="Scale">Scale Prints Ticket</option>
                                        <option value="RemotePrinter">Remote Printer (Pi)</option>
                                    </select>
                                </div>
                                <div class="col-3 d-flex align-items-end" id="printerManageSection" style="display: @(Model.RemotePrintMode == "RemotePrinter" ? "flex" : "none") !important;">
                                    <a href="/Printer" class="btn btn-outline-secondary btn-sm w-100">
                                        Printers
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Scales -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Scales</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 align-items-end">
                                <div class="col-8">
                                    <div class="form-text small mt-0">Named scales and hardware feeds live on the Scales page.</div>
                                </div>
                                <div class="col-4 d-flex align-items-end">
                                    <a href="/Scale" class="btn btn-outline-secondary btn-sm w-100">
                                        Scales
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <!-- Kiosk -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Kiosk</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="form-text small mt-0 mb-1">Kiosks register themselves and are listed on the Kiosks page.</div>
                            <div class="row g-2 mb-2">
                                <div class="col-5">
                                    <label asp-for="KioskCount" class="form-label mb-0 small"></label>
                                    <select asp-for="KioskCount" class="form-select form-select-sm" id="KioskCount">
                                        <option value="0">No Kiosk</option>
                                        <option value="1">One Kiosk</option>
                                        <option value="2">In/Out Kiosk</option>
                                    </select>
                                </div>
                                <div class="col-4" id="kioskDarkModeSection" style="display: @(Model.KioskCount > 0 ? "block" : "none");">
                                    <label asp-for="KioskDarkMode" class="form-label mb-0 small"></label>
                                    <select asp-for="KioskDarkMode" class="form-select form-select-sm">
                                        <option value="true">Dark</option>
                                        <option value="false">Light</option>
                                    </select>
                                </div>
                                <div class="col-3 d-flex align-items-end">
                                    <a href="/Kiosks" class="btn btn-outline-secondary btn-sm w-100" title="Kiosk displays that have registered themselves, and the scale, printer and reader each one uses">
                                        Kiosks
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Mobile -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Mobile</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="form-text small mt-0 mb-1">
                                The driver's phone app. With location required, a phone only weighs on the
                                scale it is standing at — set each scale's position on the Scales page.
                                Phones only share their location with an https:// site.
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="MobileRequireLocation" class="form-label mb-0 small"></label>
                                    <select asp-for="MobileRequireLocation" class="form-select form-select-sm"
                                            title="The phone must allow location and be within range of a scale with a position set">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                                <div class="col-6">
                                    <label asp-for="MobileRangeMeters" class="form-label mb-0 small"></label>
                                    <input asp-for="MobileRangeMeters" type="number" min="10" max="1000" step="5" class="form-control form-control-sm"
                                           title="How close the phone must be to the scale, in meters (10 to 1000)" />
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Cards -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Cards</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-4">
                                    <label asp-for="UseCardReader" class="form-label mb-0 small"></label>
                                    <select asp-for="UseCardReader" class="form-select form-select-sm" id="UseCardReader"
                                            title="A card carries a load's details, so the driver skips the kiosk prompts. Drivers present it at a card reader or key its number in at the kiosk.">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                                <div class="col-4" id="recycleCardsSection" style="display: @(Model.UseCardReader ? "block" : "none");">
                                    <label asp-for="RecycleCards" class="form-label mb-0 small"></label>
                                    <select asp-for="RecycleCards" class="form-select form-select-sm"
                                            title="What happens to a card when its load closes. Individual cards can override this on the Card Setup page.">
                                        <option value="false">Deactivate after each load</option>
                                        <option value="true">Recycle — card stays active</option>
                                    </select>
                                </div>
                                <div class="col-2 d-flex align-items-end" id="cardManageSection" style="display: @(Model.UseCardReader ? "flex" : "none") !important;">
                                    <a href="/Card" class="btn btn-outline-secondary btn-sm w-100" title="Register cards and issue them to drivers">
                                        Cards
                                    </a>
                                </div>
                                <div class="col-2 d-flex align-items-end" id="readerManageSection" style="display: @(Model.UseCardReader ? "flex" : "none") !important;">
                                    <a href="/Reader" class="btn btn-outline-secondary btn-sm w-100" title="Serial ports and card-read diagnostics for connected RFID Reader Services">
                                        Readers
                                    </a>
                                </div>
                            </div>
                            @* Where a card may fill in a load. Off at one place makes it
                               behave there as if cards were off; the others carry on. *@
                            <div class="row g-2 mb-2 align-items-end" id="cardSurfaceGates" style="display: @(Model.UseCardReader ? "flex" : "none");">
                                <div class="col-4">
                                    <label asp-for="AllowCardKiosk" class="form-label mb-0 small"></label>
                                    <select asp-for="AllowCardKiosk" class="form-select form-select-sm"
                                            title="Kiosks take a card at their reader or keyed in on the keypad">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                                <div class="col-4">
                                    <label asp-for="AllowCardDesktop" class="form-label mb-0 small"></label>
                                    <select asp-for="AllowCardDesktop" class="form-select form-select-sm"
                                            title="The Weigh In page has a Card box that fills in the ticket">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                                <div class="col-4">
                                    <label asp-for="AllowCardMobile" class="form-label mb-0 small"></label>
                                    <select asp-for="AllowCardMobile" class="form-select form-select-sm"
                                            title="The phone app has a Use Card button that answers the prompts">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Camera -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Camera</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-5">
                                    <label asp-for="SavePicture" class="form-label mb-0 small"></label>
                                    <select asp-for="SavePicture" class="form-select form-select-sm" id="SavePicture">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                                @* Independent of SavePicture on purpose: a site can want
                                   operators watching the lane without storing ticket photos,
                                   or the reverse. *@
                                <div class="col-4">
                                    <label asp-for="UseLiveView" class="form-label mb-0 small"></label>
                                    <select asp-for="UseLiveView" class="form-select form-select-sm" id="UseLiveView">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                                <div class="col-3 d-flex align-items-end" id="cameraManageSection" style="display: @(Model.SavePicture ? "flex" : "none") !important;">
                                    <a href="/Camera" class="btn btn-outline-secondary btn-sm w-100">
                                        Cameras
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Other -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Other</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-4">
                                    <label asp-for="UseQuickBooks" class="form-label mb-0 small"></label>
                                    <select asp-for="UseQuickBooks" class="form-select form-select-sm" id="UseQuickBooks">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                                <div class="col-2 d-flex align-items-end" id="qbCheckSection" style="display: @(Model.UseQuickBooks ? "flex" : "none") !important;">
                                    <button type="button" class="btn btn-outline-secondary btn-sm w-100" id="btnCheckQBService" onclick="checkQBService()">
                                        Check
                                    </button>
                                </div>
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-4">
                                    <label asp-for="UseRetainedTare" class="form-label mb-0 small"></label>
                                    <select asp-for="UseRetainedTare" class="form-select form-select-sm" id="UseRetainedTare">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                                <div class="col-4" id="autoClearStaleSection" style="display: @(Model.UseRetainedTare ? "block" : "none");">
                                    <label asp-for="AutoClearStaleRetainedTare" class="form-label mb-0 small"></label>
                                    <select asp-for="AutoClearStaleRetainedTare" class="form-select form-select-sm">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                                <div class="col-2 d-flex align-items-end" id="retainedTareManageSection" style="display: @(Model.UseRetainedTare ? "flex" : "none") !important;">
                                    <a href="/MasterData?tab=carriersTab" class="btn btn-outline-secondary btn-sm w-100" title="Manage on MasterData → Carriers &amp; Trucks">
                                        Manage
                                    </a>
                                </div>
                            </div>
                            @* Who may throw a stored tare away. Off means that source stops
                               offering the choice — the kiosk and card flows then apply the
                               tare automatically, and the phone still lets a driver skip it
                               for one load without clearing it. *@
                            <div class="row g-2 mb-2" id="tareResetGates" style="display: @(Model.UseRetainedTare ? "flex" : "none");">
                                <div class="col-4">
                                    <label asp-for="AllowTareResetKiosk" class="form-label mb-0 small"></label>
                                    <select asp-for="AllowTareResetKiosk" class="form-select form-select-sm">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                                <div class="col-4">
                                    <label asp-for="AllowTareResetMobile" class="form-label mb-0 small"></label>
                                    <select asp-for="AllowTareResetMobile" class="form-select form-select-sm">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                                <div class="col-4">
                                    <label asp-for="AllowTareResetCard" class="form-label mb-0 small"></label>
                                    <select asp-for="AllowTareResetCard" class="form-select form-select-sm">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-4">
                                    <label asp-for="UseBinInventory" class="form-label mb-0 small"></label>
                                    <select asp-for="UseBinInventory" class="form-select form-select-sm" id="UseBinInventory">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                                <div class="col-2 d-flex align-items-end" id="binManageSection" style="display: @(Model.UseBinInventory ? "flex" : "none") !important;">
                                    <a href="/MasterData?tab=binsTab" class="btn btn-outline-secondary btn-sm w-100" title="Manage bins on Tables → Bins">
                                        Bins
                                    </a>
                                </div>
                                <div class="col-3" id="binRequiredSection" style="display: @(Model.UseBinInventory ? "block" : "none");">
                                    <label asp-for="BinRequired" class="form-label mb-0 small"></label>
                                    <select asp-for="BinRequired" class="form-select form-select-sm" id="BinRequired"
                                            title="Required blocks saving a ticket without a bin and removes Skip from the kiosk bin prompt">
                                        <option value="false">Optional</option>
                                        <option value="true">Required</option>
                                    </select>
                                </div>
                                <div class="col-3" id="binLockSection" style="display: @(Model.UseBinInventory ? "block" : "none");">
                                    <label asp-for="BinCommodityLock" class="form-label mb-0 small"></label>
                                    <select asp-for="BinCommodityLock" class="form-select form-select-sm" id="BinCommodityLock"
                                            title="A bin holding grain only accepts that commodity — picking the bin auto-sets it on the ticket. The bin frees up when it's emptied or trued up to zero.">
                                        <option value="false">No</option>
                                        <option value="true">Yes</option>
                                    </select>
                                </div>
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-4">
                                    <label asp-for="DefaultReportUnit" class="form-label mb-0 small"></label>
                                    <select asp-for="DefaultReportUnit" class="form-select form-select-sm"
                                            title="Report unit columns use each commodity's Reporting Unit (Tables → Commodities); commodities without one fall back to this">
                                        <option value="lbs">Pounds (lbs)</option>
                                        <option value="kg">Kilograms (kg)</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Driver Signature -->
                    <div class="card mb-3">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Driver Signature</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="SignatureMode" class="form-label mb-0 small"></label>
                                    <select asp-for="SignatureMode" class="form-select form-select-sm" id="SignatureMode">
                                        <option value="None">Off</option>
                                        <option value="Operator">Operator Device (overlay)</option>
                                        <option value="RemotePad">Remote Signature Pad</option>
                                    </select>
                                </div>
                                <div class="col-3 signature-option" style="display: @(Model.SignatureMode != "None" ? "block" : "none");">
                                    <label asp-for="SignatureRequired" class="form-label mb-0 small"></label>
                                    <select asp-for="SignatureRequired" class="form-select form-select-sm">
                                        <option value="false">Optional</option>
                                        <option value="true">Required</option>
                                    </select>
                                </div>
                                <div class="col-3 signature-option" style="display: @(Model.SignatureMode != "None" ? "block" : "none");">
                                    <label asp-for="PrintSignatureOnTicket" class="form-label mb-0 small"></label>
                                    <select asp-for="PrintSignatureOnTicket" class="form-select form-select-sm">
                                        <option value="true">Yes</option>
                                        <option value="false">No</option>
                                    </select>
                                </div>
                            </div>
                            <div class="row g-2 mb-1" id="signaturePadIdSection" style="display: @(Model.SignatureMode == "RemotePad" ? "flex" : "none");">
                                <div class="col-6">
                                    <label asp-for="SignaturePadId" class="form-label mb-0 small"></label>
                                    <input asp-for="SignaturePadId" class="form-control form-control-sm" placeholder="default" />
                                </div>
                                <div class="col-6 d-flex align-items-center gap-2">
                                    <img id="signaturePadQr" src="" alt="Scan to open the signature pad"
                                         title="Scan with the tablet to open the signature pad"
                                         style="width: 96px; height: 96px; background: #fff; border-radius: 4px;" />
                                    <span class="small text-muted">Scan with the tablet, or open:<br />
                                        <a id="signaturePadLink" href="#" target="_blank" rel="noopener"><code id="signaturePadUrl"></code></a>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Fields -->
        <div class="tab-pane fade" id="tabFields">
            <div class="row g-3">
                <div class="col-lg-5 col-xl-4">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Standard Fields</strong>
                        </div>
                        <div class="card-body py-2">
                            <p class="small mb-2" style="color: var(--bw-text-muted);">
                                Hidden fields disappear from the weigh forms, grids, reports,
                                printed tickets, and kiosk prompts. Stored values are kept
                                but not shown. Weights, dates, and the ticket number are
                                always visible.
                            </p>
                            <table class="table table-sm align-middle mb-2">
                                <thead>
                                    <tr class="small text-muted"><th>Field</th><th style="width:38%;">Visibility</th><th style="width:22%;">Order</th></tr>
                                </thead>
                                <tbody>
                                    @{
                                        var visibilityRows = new (string Label, string Prop, bool Hidden, string OrderProp, int Order)[]
                                        {
                                            ("Customer", "HideCustomer", Model.HideCustomer, "FieldOrderCustomer", Model.FieldOrderCustomer),
                                            ("Carrier", "HideCarrier", Model.HideCarrier, "FieldOrderCarrier", Model.FieldOrderCarrier),
                                            ("Truck ID", "HideTruckId", Model.HideTruckId, "FieldOrderTruckId", Model.FieldOrderTruckId),
                                            ("Commodity", "HideCommodity", Model.HideCommodity, "FieldOrderCommodity", Model.FieldOrderCommodity),
                                            ("Location", "HideLocation", Model.HideLocation, "FieldOrderLocation", Model.FieldOrderLocation),
                                            ("Destination", "HideDestination", Model.HideDestination, "FieldOrderDestination", Model.FieldOrderDestination),
                                            ("Notes", "HideNotes", Model.HideNotes, "FieldOrderNotes", Model.FieldOrderNotes),
                                        };
                                    }
                                    @foreach (var (label, prop, hidden, orderProp, order) in visibilityRows)
                                    {
                                        <tr>
                                            <td class="fw-bold">@label</td>
                                            <td>
                                                <select name="@prop" id="@prop" class="form-select form-select-sm field-visibility">
                                                    <option value="false" selected="@(!hidden ? "selected" : null)">Visible</option>
                                                    <option value="true" selected="@(hidden ? "selected" : null)">Hidden</option>
                                                </select>
                                            </td>
                                            <td>
                                                <input type="number" name="@orderProp" id="@orderProp" value="@order" step="1"
                                                       class="form-control form-control-sm" />
                                            </td>
                                        </tr>
                                    }
                                    @* Bin has no Hide flag — the Bin Inventory option (System → Options)
                                       controls its visibility, so only the order is editable here. *@
                                    <tr id="binFieldOrderRow" style="display: @(Model.UseBinInventory ? "table-row" : "none");">
                                        <td class="fw-bold">Bin</td>
                                        <td>
                                            <span class="badge bg-info text-dark" title="Shown while Bin Inventory is enabled (System → Options)">Bin Inventory</span>
                                        </td>
                                        <td>
                                            <input type="number" name="FieldOrderBin" id="FieldOrderBin" value="@Model.FieldOrderBin" step="1"
                                                   class="form-control form-control-sm" />
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                            <div class="form-text small">
                                Hiding <strong>Carrier</strong> also hides <strong>Truck ID</strong>
                                (trucks belong to carriers). While <strong>Retained Tare</strong>
                                is enabled, Carrier and Truck ID stay visible.
                                <strong>Order</strong> sorts fields on the weigh forms — standard
                                and custom fields share one scale, so a custom field can slot
                                between the built-ins. Printed-ticket layout is controlled by the
                                Ticket Designers.
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-lg-7 col-xl-6">
                    <div class="card">
                        <div class="card-header py-1 d-flex justify-content-between align-items-center" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Custom Fields</strong>
                            <button type="button" class="btn btn-outline-primary btn-sm py-0" id="addCustomFieldBtn">+ Add Field</button>
                        </div>
                        <div class="card-body py-2">
                            <p class="small mb-2" style="color: var(--bw-text-muted);">
                                Custom fields appear on the Weigh In / Weigh Out / Edit / Basic
                                Ticket forms, as grid columns, and on printed tickets. Dropdown
                                fields also get their own tab on Tables. Order shares one
                                scale with the standard fields.
                            </p>
                            <table class="table table-sm align-middle mb-0" id="customFieldsTable">
                                <thead>
                                    <tr class="small text-muted">
                                        <th>Name</th><th>Type</th><th>Entry</th><th>On Ticket</th><th>Order</th><th>Active</th><th></th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                            <div id="customFieldsEmpty" class="small text-muted" style="display:none;">No custom fields defined.</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Locations (physical sites) -->
        <div class="tab-pane fade" id="tabLocations">
            <div class="row g-3">
                <div class="col-12 col-xxl-10">
                    <div class="card">
                        <div class="card-header py-1 d-flex justify-content-between align-items-center" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Locations</strong>
                            <button type="button" class="btn btn-outline-primary btn-sm py-0" id="addLocationBtn"><i class="fa fa-plus"></i> Add Location</button>
                        </div>
                        <div class="card-body py-2" id="locationsEditor">
                            <p class="small mb-2" style="color: var(--bw-text-muted);">
                                Optional physical locations (yards/facilities). With two or more
                                active locations, a picker appears in the navbar — each operator
                                chooses where they are, and that workstation only sees its
                                location's scales. Assign scales to locations on the
                                <a href="/Scale">Scales page</a>; bins and commodities can be
                                limited to a location on <a href="/MasterData">Tables</a>.
                                Leave this empty for a single-location setup. Changes save
                                immediately.
                            </p>
                            <table class="table table-sm align-middle mb-0" id="sitesTable">
                                <thead>
                                    <tr class="small text-muted">
                                        <th>Name</th><th>Address</th><th style="width:130px;">City</th>
                                        <th style="width:70px;">State</th><th style="width:90px;">Zip</th>
                                        <th style="width:130px;">Phone</th>
                                        <th style="width:80px;">Order</th>
                                        <th style="width:60px;" class="text-center">Active</th>
                                        <th style="width:70px;"></th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                            <div id="siteStatus" class="small mt-1">&nbsp;</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @* TAB: Email — SMTP settings, scheduled Excel reports, per-load emails.
           Everything in here saves through /api/email/*, not the settings form's
           auto-save, so #emailEditor is excluded from that handler. *@
        <div class="tab-pane fade" id="tabEmail">
            <div id="emailEditor">
                <div class="row g-3">
                    <div class="col-12 col-xl-5">
                        <div class="card">
                            <div class="card-header py-1 d-flex justify-content-between align-items-center" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                                <strong>Email Server</strong>
                                <div class="form-check form-switch mb-0">
                                    <input class="form-check-input" type="checkbox" id="emEnabled" />
                                    <label class="form-check-label small" for="emEnabled">Enabled</label>
                                </div>
                            </div>
                            <div class="card-body py-2">
                                <p class="small mb-2" style="color: var(--bw-text-muted);">
                                    Outgoing mail for scheduled reports and load emails. Defaults are
                                    for <a href="https://www.smtp2go.com" target="_blank" rel="noopener">SMTP2GO</a>
                                    — use the SMTP username and password from your SMTP2GO account
                                    (not your login). Any relay works. Changes save when you leave a
                                    field.
                                </p>
                                <div class="row g-2">
                                    <div class="col-8">
                                        <label class="form-label mb-0 small" for="emHost">SMTP Server</label>
                                        <input class="form-control form-control-sm" id="emHost" maxlength="200" placeholder="mail.smtp2go.com" />
                                    </div>
                                    <div class="col-4">
                                        <label class="form-label mb-0 small" for="emPort">Port</label>
                                        <input class="form-control form-control-sm" id="emPort" type="number" min="1" max="65535" />
                                    </div>
                                    <div class="col-12">
                                        <label class="form-label mb-0 small" for="emSecurity">Security</label>
                                        <select class="form-select form-select-sm" id="emSecurity">
                                            <option value="StartTls">STARTTLS (ports 2525, 587, 25)</option>
                                            <option value="SslOnConnect">SSL/TLS (port 465)</option>
                                            <option value="None">None (unencrypted)</option>
                                        </select>
                                    </div>
                                    <div class="col-6">
                                        <label class="form-label mb-0 small" for="emUsername">Username</label>
                                        <input class="form-control form-control-sm" id="emUsername" maxlength="200" autocomplete="off" />
                                    </div>
                                    <div class="col-6">
                                        <label class="form-label mb-0 small" for="emPassword">Password</label>
                                        <input class="form-control form-control-sm" id="emPassword" type="password" maxlength="200" autocomplete="new-password" />
                                    </div>
                                    <div class="col-7">
                                        <label class="form-label mb-0 small" for="emFromAddress">From Address</label>
                                        <input class="form-control form-control-sm" id="emFromAddress" maxlength="200" placeholder="scale@yourcompany.com" />
                                    </div>
                                    <div class="col-5">
                                        <label class="form-label mb-0 small" for="emFromName">From Name</label>
                                        <input class="form-control form-control-sm" id="emFromName" maxlength="100" />
                                    </div>
                                    <div class="col-12">
                                        <label class="form-label mb-0 small" for="emReplyTo">Reply-To <span class="text-muted">(optional)</span></label>
                                        <input class="form-control form-control-sm" id="emReplyTo" maxlength="200" />
                                    </div>
                                </div>

                                <hr class="my-2" />
                                <label class="form-label mb-0 small" for="emTestTo">Send a test message to</label>
                                <div class="input-group input-group-sm">
                                    <input class="form-control form-control-sm" id="emTestTo" placeholder="you@yourcompany.com" />
                                    <button type="button" class="btn btn-outline-primary" id="emTestBtn">Send Test</button>
                                </div>
                                <div id="emSettingsStatus" class="small mt-1">&nbsp;</div>
                            </div>
                        </div>
                    </div>

                    <div class="col-12 col-xl-7">
                        <div class="card mb-3">
                            <div class="card-header py-1 d-flex justify-content-between align-items-center" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                                <strong>Scheduled Reports</strong>
                                <button type="button" class="btn btn-outline-primary btn-sm py-0" id="emAddScheduleBtn"><i class="fa fa-plus"></i> Add Schedule</button>
                            </div>
                            <div class="card-body py-2">
                                <p class="small mb-2" style="color: var(--bw-text-muted);">
                                    Excel workbooks of what was weighed, emailed daily, weekly or
                                    monthly. Each schedule covers the last completed period and can
                                    go to as many addresses as you like. Columns follow the fields
                                    you've enabled on the <strong>Fields</strong> tab, plus net
                                    pounds and each commodity's own unit.
                                </p>
                                <div class="table-responsive">
                                    <table class="table table-sm align-middle mb-0" id="emSchedulesTable">
                                        <thead>
                                            <tr class="small text-muted">
                                                <th>Name</th><th>When</th><th>Recipients</th>
                                                <th>Filters</th><th>Next Run</th><th>Status</th>
                                                <th style="width:150px;"></th>
                                            </tr>
                                        </thead>
                                        <tbody></tbody>
                                    </table>
                                </div>
                                <div id="emScheduleStatus" class="small mt-1">&nbsp;</div>
                            </div>
                        </div>

                        <div class="card">
                            <div class="card-header py-1 d-flex justify-content-between align-items-center" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                                <strong>Individual Load Emails</strong>
                                <button type="button" class="btn btn-outline-primary btn-sm py-0" id="emAddRuleBtn"><i class="fa fa-plus"></i> Add Rule</button>
                            </div>
                            <div class="card-body py-2">
                                <p class="small mb-2" style="color: var(--bw-text-muted);">
                                    Emails one message per load as soon as it's weighed out. Filter by
                                    customer, by commodity, by both, or by neither — leaving a filter
                                    empty matches everything. Several rules can match the same load;
                                    each sends its own email.
                                </p>
                                <div class="table-responsive">
                                    <table class="table table-sm align-middle mb-0" id="emRulesTable">
                                        <thead>
                                            <tr class="small text-muted">
                                                <th>Name</th><th>Recipients</th><th>Customers</th>
                                                <th>Commodities</th><th>Last Sent</th>
                                                <th style="width:60px;" class="text-center">Active</th>
                                                <th style="width:110px;"></th>
                                            </tr>
                                        </thead>
                                        <tbody></tbody>
                                    </table>
                                </div>
                                <div id="emRuleStatus" class="small mt-1">&nbsp;</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Kiosk Prompts -->
        <div class="tab-pane fade" id="tabKioskPrompts">
            <div id="retainedTareRequiresPrompts" class="alert alert-info py-1 mb-2 small"
                 style="display: @(Model.UseRetainedTare ? "block" : "none");" role="alert">
                <i class="fa fa-circle-info"></i>
                <strong>Retained Tare requires</strong> the <em>Carrier</em> and <em>Truck&nbsp;ID</em>
                prompts to be enabled — they identify the truck so the stored tare can be
                recalled. Outbound prompts are disabled because the weigh-out is
                auto-completed via the recalled tare — they would never fire. Both rules
                stay in effect while Retained Tare is enabled in
                <strong>System &rarr; Options</strong>.
            </div>
            <div class="row">
                <div class="col-lg-7 col-xl-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Kiosk Prompts</strong>
                        </div>
                        <div class="card-body py-2">
                            @{
                                string Direction(bool inb, bool outb) =>
                                    inb ? "inbound" : (outb ? "outbound" : "neither");
                            }
                            <table class="table table-sm align-middle mb-0">
                                <thead>
                                    <tr class="small text-muted">
                                        <th>Prompt</th>
                                        <th class="text-center" style="width:50%;">When prompted</th>
                                        <th class="text-center">Allow Skip</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td class="fw-bold">
                                            Carrier
                                            <span id="PromptKioskCarrierLock" class="badge bg-info text-dark ms-1"
                                                  style="display: @(Model.UseRetainedTare ? "inline" : "none");"
                                                  title="Required while Retained Tare is enabled">required</span>
                                        </td>
                                        <td class="text-center">
                                            <div class="form-check form-check-inline">
                                                <input class="form-check-input" type="radio" name="kioskCarrierDir" id="carrierDirNone"
                                                       value="neither" @(!Model.PromptKioskCarrier ? "checked" : "") />
                                                <label class="form-check-label small" for="carrierDirNone">Neither</label>
                                            </div>
                                            <div class="form-check form-check-inline">
                                                <input class="form-check-input" type="radio" name="kioskCarrierDir" id="carrierDirInbound"
                                                       value="inbound" @(Model.PromptKioskCarrier ? "checked" : "") />
                                                <label class="form-check-label small" for="carrierDirInbound">Inbound</label>
                                            </div>
                                            <input type="hidden" asp-for="PromptKioskCarrier" id="PromptKioskCarrier" />
                                        </td>
                                        <td class="text-center">
                                            <input asp-for="AllowSkipCarrier" class="form-check-input" id="AllowSkipCarrier" />
                                        </td>
                                    </tr>
                                    <tr id="truckIdPromptSection" style="display: @(Model.PromptKioskCarrier ? "table-row" : "none");">
                                        <td class="fw-bold">
                                            Truck ID
                                            <span id="PromptKioskTruckIdLock" class="badge bg-info text-dark ms-1"
                                                  style="display: @(Model.UseRetainedTare ? "inline" : "none");"
                                                  title="Required while Retained Tare is enabled">required</span>
                                        </td>
                                        <td class="text-center">
                                            <div class="form-check form-check-inline">
                                                <input class="form-check-input" type="radio" name="kioskTruckDir" id="truckDirNone"
                                                       value="neither" @(!Model.PromptKioskTruckId ? "checked" : "") />
                                                <label class="form-check-label small" for="truckDirNone">Neither</label>
                                            </div>
                                            <div class="form-check form-check-inline">
                                                <input class="form-check-input" type="radio" name="kioskTruckDir" id="truckDirInbound"
                                                       value="inbound" @(Model.PromptKioskTruckId ? "checked" : "") />
                                                <label class="form-check-label small" for="truckDirInbound">Inbound</label>
                                            </div>
                                            <input type="hidden" asp-for="PromptKioskTruckId" id="PromptKioskTruckId" />
                                        </td>
                                        <td class="text-center">
                                            <input asp-for="AllowSkipTruckId" class="form-check-input" id="AllowSkipTruckId" />
                                        </td>
                                    </tr>
                                    @{
                                        var splitPrompts = new[]
                                        {
                                            ("Commodity", "kioskCommodityDir", "PromptKioskCommodityOnInbound", "PromptKioskCommodityOnOutbound", "AllowSkipCommodity",
                                                Direction(Model.PromptKioskCommodityOnInbound, Model.PromptKioskCommodityOnOutbound),
                                                Model.PromptKioskCommodityOnInbound, Model.PromptKioskCommodityOnOutbound, Model.AllowSkipCommodity),
                                            ("Customer", "kioskCustomerDir", "PromptKioskCustomerOnInbound", "PromptKioskCustomerOnOutbound", "AllowSkipCustomer",
                                                Direction(Model.PromptKioskCustomerOnInbound, Model.PromptKioskCustomerOnOutbound),
                                                Model.PromptKioskCustomerOnInbound, Model.PromptKioskCustomerOnOutbound, Model.AllowSkipCustomer),
                                            ("Location", "kioskLocationDir", "PromptKioskLocationOnInbound", "PromptKioskLocationOnOutbound", "AllowSkipLocation",
                                                Direction(Model.PromptKioskLocationOnInbound, Model.PromptKioskLocationOnOutbound),
                                                Model.PromptKioskLocationOnInbound, Model.PromptKioskLocationOnOutbound, Model.AllowSkipLocation),
                                            ("Destination", "kioskDestDir", "PromptKioskDestinationOnInbound", "PromptKioskDestinationOnOutbound", "AllowSkipDestination",
                                                Direction(Model.PromptKioskDestinationOnInbound, Model.PromptKioskDestinationOnOutbound),
                                                Model.PromptKioskDestinationOnInbound, Model.PromptKioskDestinationOnOutbound, Model.AllowSkipDestination),
                                        };
                                    }
                                    @{
                                        // Bin rides the same split-prompt machinery; its row is hidden
                                        // while Bin Inventory is off (System → Options) and toggled
                                        // live by the UseBinInventory change handler.
                                        splitPrompts = splitPrompts.Append(
                                            ("Bin", "kioskBinDir", "PromptKioskBinOnInbound", "PromptKioskBinOnOutbound", "AllowSkipBin",
                                                Direction(Model.PromptKioskBinOnInbound, Model.PromptKioskBinOnOutbound),
                                                Model.PromptKioskBinOnInbound, Model.PromptKioskBinOnOutbound, Model.AllowSkipBin)).ToArray();
                                    }
                                    @foreach (var (label, group, inField, outField, skipField, currentDir, inVal, outVal, skipVal) in splitPrompts)
                                    {
                                        <tr id="@(label == "Bin" ? "binPromptRow" : null)"
                                            style="@(label == "Bin" && !Model.UseBinInventory ? "display: none;" : null)">
                                            <td class="fw-bold">@label</td>
                                            <td class="text-center">
                                                <div class="form-check form-check-inline">
                                                    <input class="form-check-input" type="radio" name="@group" id="@(group)None"
                                                           value="neither" @(currentDir == "neither" ? "checked" : "") />
                                                    <label class="form-check-label small" for="@(group)None">Neither</label>
                                                </div>
                                                <div class="form-check form-check-inline">
                                                    <input class="form-check-input" type="radio" name="@group" id="@(group)Inbound"
                                                           value="inbound" @(currentDir == "inbound" ? "checked" : "") />
                                                    <label class="form-check-label small" for="@(group)Inbound">Inbound</label>
                                                </div>
                                                <div class="form-check form-check-inline">
                                                    <input class="form-check-input" type="radio" name="@group" id="@(group)Outbound"
                                                           value="outbound" @(currentDir == "outbound" ? "checked" : "") />
                                                    <label class="form-check-label small" for="@(group)Outbound">Outbound</label>
                                                </div>
                                                <input type="hidden" name="@inField" id="@inField" value="@inVal.ToString().ToLower()" />
                                                <input type="hidden" name="@outField" id="@outField" value="@outVal.ToString().ToLower()" />
                                            </td>
                                            <td class="text-center">
                                                <input type="checkbox" name="@skipField" id="@skipField" class="form-check-input" value="true" @(skipVal ? "checked" : "") />
                                                <input type="hidden" name="@skipField" value="false" />
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <hr class="my-2" />
                            <div class="form-check">
                                <input asp-for="HideKioskOnScreenButtons" class="form-check-input" id="HideKioskOnScreenButtons" />
                                <label asp-for="HideKioskOnScreenButtons" class="form-check-label small"></label>
                                <div class="form-text small">
                                    Hides the numpad, Cancel, Skip, Select, and Done buttons on
                                    the kiosk. Operator drives with a physical keyboard or
                                    barcode scanner. The New Load / Enter Ticket buttons
                                    remain visible.
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Users & Security -->
        <div class="tab-pane fade" id="tabSecurity">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Users &amp; Security</strong>
                        </div>
                        <div class="card-body py-2">
                            <a asp-controller="Account" asp-action="Users" class="btn btn-outline-primary btn-sm w-100 mb-2">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="me-1">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 19.128a9.38 9.38 0 002.625.372 9.337 9.337 0 004.121-.952 4.125 4.125 0 00-7.533-2.493M15 19.128v-.003c0-1.113-.285-2.16-.786-3.07M15 19.128v.106A12.318 12.318 0 018.624 21c-2.331 0-4.512-.645-6.374-1.766l-.001-.109a6.375 6.375 0 0111.964-3.07M12 6.375a3.375 3.375 0 11-6.75 0 3.375 3.375 0 016.75 0zm8.25 2.25a2.625 2.625 0 11-5.25 0 2.625 2.625 0 015.25 0z" />
                                </svg>
                                Manage Users
                            </a>
                            <div class="small" style="color: var(--bw-text-muted);">
                                <p class="mb-1"><strong>Kiosk PIN:</strong> <code id="kioskPinDisplay">@Model.KioskCode</code></p>
                                <p class="mb-1">When login is enabled, kiosks need the PIN to access the scale screen. Pass it as a URL parameter:</p>
                                <code class="d-block mb-1" style="font-size: 0.75rem; word-break: break-all;">https://your-server/Kiosk?pin=<span id="kioskPinUrl">@Model.KioskCode</span></code>
                                <p class="mb-0">The PIN is stored as a cookie after the first visit so subsequent requests don't need it.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Ticket Designers -->
        <div class="tab-pane fade" id="tabDesigners">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Ticket Designers</strong>
                        </div>
                        <div class="card-body py-2">
                            <p class="small mb-2" style="color: var(--bw-text-muted);">Edit ticket layouts. Changes are saved and used when printing.</p>
                            <div class="d-flex align-items-center gap-2 mb-2">
                                <a asp-controller="Ticket" asp-action="Designer" asp-route-reportName="TicketReport"
                                   class="btn btn-outline-primary btn-sm d-flex align-items-center gap-1 flex-grow-1">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 6v.75m0 3v.75m0 3v.75m0 3V18m-9-5.25h5.25M7.5 15h3M3.375 5.25c-.621 0-1.125.504-1.125 1.125v3.026a2.999 2.999 0 010 5.198v3.026c0 .621.504 1.125 1.125 1.125h17.25c.621 0 1.125-.504 1.125-1.125v-3.026a2.999 2.999 0 010-5.198V6.375c0-.621-.504-1.125-1.125-1.125H3.375z" />
                                    </svg>
                                    <span class="fw-bold">Completed Ticket</span>
                                </a>
                            </div>
                            <div class="d-flex align-items-center gap-2 mb-2">
                                <a asp-controller="Ticket" asp-action="Designer" asp-route-reportName="KioskTicketReport"
                                   class="btn btn-outline-primary btn-sm d-flex align-items-center gap-1 flex-grow-1">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 013.75 9.375v-4.5zM3.75 14.625c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5a1.125 1.125 0 01-1.125-1.125v-4.5zM13.5 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 0113.5 9.375v-4.5z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 14.625v5.25h6v-5.25h-6z" />
                                    </svg>
                                    <span class="fw-bold">Kiosk Inbound Ticket</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Reset Ticket Layouts</strong>
                        </div>
                        <div class="card-body py-2">
                            <p class="small mb-2" style="color: var(--bw-text-muted);">Reset ticket layouts to default format. Customizations will be lost.</p>
                            <div class="d-flex gap-2">
                                <button type="button" class="btn btn-outline-danger btn-sm"
                                        onclick="showConfirmModal('Reset Completed Ticket', 'Reset Completed Ticket to default format?\n\nAny customizations will be lost.', 'resetTicketForm', 'btn-danger')">
                                    Reset Completed Ticket
                                </button>
                                <button type="button" class="btn btn-outline-danger btn-sm"
                                        onclick="showConfirmModal('Reset Kiosk Ticket', 'Reset Kiosk Inbound Ticket to default format?\n\nAny customizations will be lost.', 'resetKioskTicketForm', 'btn-danger')">
                                    Reset Kiosk Ticket
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: API -->
        <div class="tab-pane fade" id="tabApi">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>API Documentation</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="mb-2">
                                <label asp-for="ApiDefinitionPin" class="form-label mb-0 small"></label>
                                <div class="d-flex gap-2 align-items-center">
                                    <input asp-for="ApiDefinitionPin" class="form-control form-control-sm" maxlength="20" id="ApiDefinitionPin" style="max-width: 200px;" />
                                    <a id="swaggerLink" href="/swagger?pin=@Model.ApiDefinitionPin" target="_blank" class="btn btn-outline-info btn-sm text-nowrap">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16" class="me-1">
                                            <path d="M4.715 6.542 3.343 7.914a3 3 0 1 0 4.243 4.243l1.828-1.829A3 3 0 0 0 8.586 5.5L8 6.086a1 1 0 0 0-.154.199 2 2 0 0 1 .861 3.337L6.88 11.45a2 2 0 1 1-2.83-2.83l.793-.792a4 4 0 0 1-.128-1.287z"/>
                                            <path d="M6.586 4.672A3 3 0 0 0 7.414 9.5l.775-.776a2 2 0 0 1-.896-3.346L9.12 3.55a2 2 0 1 1 2.83 2.83l-.793.792c.112.42.155.855.128 1.287l1.372-1.372a3 3 0 1 0-4.243-4.243z"/>
                                        </svg>
                                        Open Swagger
                                    </a>
                                </div>
                                <small class="text-muted">This PIN is required to access the Swagger API documentation page.</small>
                            </div>
                            <div class="small mt-2" style="color: var(--bw-text-muted);">
                                <p class="mb-1"><strong>Swagger URL:</strong></p>
                                <code class="d-block mb-1" style="font-size: 0.75rem; word-break: break-all;">https://your-server/swagger?pin=<span id="apiPinUrl">@Model.ApiDefinitionPin</span></code>
                                <p class="mb-0">The PIN is stored as a cookie after the first visit so subsequent requests don't need it.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Services -->
        <div class="tab-pane fade" id="tabServices">
            <div class="row g-3">
                <div class="col-md-9">
                    <div class="card">
                        <div class="card-header py-1 d-flex align-items-center justify-content-between" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Connected Services</strong>
                            <button type="button" class="btn btn-outline-secondary btn-sm py-0" id="btnRefreshServices">
                                <i class="fa fa-refresh"></i> Refresh
                            </button>
                        </div>
                        <div class="card-body py-2">
                            <div class="small mb-2" style="color: var(--bw-text-muted);">
                                This server is running <strong><span id="serverVersion">—</span></strong>.
                                Everything below is connected to it right now. A service that is powered
                                off or off the network does not appear at all.
                            </div>
                            <div class="table-responsive">
                                <table class="table table-sm align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th>Service</th>
                                            <th>ID</th>
                                            <th>Version</th>
                                            <th>Status</th>
                                        </tr>
                                    </thead>
                                    <tbody id="servicesTableBody">
                                        <tr><td colspan="4" class="text-muted small">Loading…</td></tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB: Database -->
        @if (ViewBag.ShowResetDatabase == true)
        {
            <div class="tab-pane fade" id="tabDatabase">
                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                                <strong>Database</strong>
                            </div>
                            <div class="card-body py-2">
                                <p class="small mb-2" style="color: var(--bw-text-muted);">Clear all data or reset with sample test data. Settings are preserved.<br /><strong>Clear Transactions</strong> removes only the weighing history and keeps your master data.</p>
                                <div class="d-flex gap-2">
                                    <button type="button" class="btn btn-outline-warning btn-sm" onclick="new bootstrap.Modal(document.getElementById('sampleDataModal')).show()">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="me-1">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M20.25 6.375c0 2.278-3.694 4.125-8.25 4.125S3.75 8.653 3.75 6.375m16.5 0c0-2.278-3.694-4.125-8.25-4.125S3.75 4.097 3.75 6.375m16.5 0v11.25c0 2.278-3.694 4.125-8.25 4.125s-8.25-1.847-8.25-4.125V6.375m16.5 0v3.75m-16.5-3.75v3.75m16.5 0v3.75C20.25 16.153 16.556 18 12 18s-8.25-1.847-8.25-4.125v-3.75m16.5 0c0 2.278-3.694 4.125-8.25 4.125s-8.25-1.847-8.25-4.125" />
                                        </svg>
                                        Load Test Data
                                    </button>
                                    <button type="button" class="btn btn-outline-danger btn-sm" onclick="showConfirmModal('Clear Transactions', 'This will DELETE every ticket, along with its custom field values, ticket images and signatures.\n\nCustomers, carriers, commodities, locations, destinations, trucks and bins are KEPT. The ticket number restarts at 1.\n\nThis cannot be undone.', 'clearTransactionsForm', 'btn-danger')">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="me-1"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 002.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 00-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 00.75-.75 2.25 2.25 0 00-.1-.664m-5.8 0A2.251 2.251 0 0113.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25z" /></svg>
                                        Clear Transactions
                                    </button>
                                    <button type="button" class="btn btn-outline-danger btn-sm" onclick="showConfirmModal('Clear Database', 'This will DELETE all transactions, customers, carriers, commodities, locations, destinations, and trucks.\n\nThis cannot be undone.', 'clearDatabaseForm', 'btn-danger')">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="me-1">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 01-2.244 2.077H8.084a2.25 2.25 0 01-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 00-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 013.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 00-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 00-7.5 0" />
                                        </svg>
                                        Clear Database
                                    </button>
                                </div>
                                <p class="small text-muted mt-2 mb-0">
                                    <em>Hide this section by setting <code>"ShowResetDatabase": false</code> in appsettings.json</em>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    @* No Save button — every change auto-saves (see the auto-save script). *@
</form>

@* Scheduled-report editor. Outside the settings form so its controls can't
   trigger the form's auto-save. *@
<div class="modal fade" id="emScheduleModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header py-2">
                <h6 class="modal-title" id="emScheduleModalTitle">Scheduled Report</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body py-2">
                <div class="row g-2">
                    <div class="col-8">
                        <label class="form-label mb-0 small" for="emsName">Report Name</label>
                        <input class="form-control form-control-sm" id="emsName" maxlength="100" />
                    </div>
                    <div class="col-4 d-flex align-items-end">
                        <div class="form-check form-switch mb-1">
                            <input class="form-check-input" type="checkbox" id="emsActive" checked />
                            <label class="form-check-label small" for="emsActive">Active</label>
                        </div>
                    </div>

                    <div class="col-12">
                        <label class="form-label mb-0 small" for="emsRecipients">To</label>
                        <textarea class="form-control form-control-sm" id="emsRecipients" rows="2"
                                  placeholder="office@yourcompany.com, owner@yourcompany.com"></textarea>
                        <div class="form-text small mt-0">Separate addresses with commas, semicolons or new lines.</div>
                    </div>
                    <div class="col-12">
                        <label class="form-label mb-0 small" for="emsCc">CC <span class="text-muted">(optional)</span></label>
                        <input class="form-control form-control-sm" id="emsCc" />
                    </div>

                    <div class="col-12"><hr class="my-1" /></div>

                    <div class="col-4">
                        <label class="form-label mb-0 small" for="emsFrequency">Frequency</label>
                        <select class="form-select form-select-sm" id="emsFrequency">
                            <option value="Daily">Daily</option>
                            <option value="Weekly">Weekly</option>
                            <option value="Monthly">Monthly</option>
                        </select>
                    </div>
                    <div class="col-4" id="emsDayOfWeekWrap">
                        <label class="form-label mb-0 small" for="emsDayOfWeek">Day</label>
                        <select class="form-select form-select-sm" id="emsDayOfWeek">
                            <option value="0">Sunday</option>
                            <option value="1">Monday</option>
                            <option value="2">Tuesday</option>
                            <option value="3">Wednesday</option>
                            <option value="4">Thursday</option>
                            <option value="5">Friday</option>
                            <option value="6">Saturday</option>
                        </select>
                    </div>
                    <div class="col-4" id="emsDayOfMonthWrap">
                        <label class="form-label mb-0 small" for="emsDayOfMonth">Day of Month</label>
                        <input class="form-control form-control-sm" id="emsDayOfMonth" type="number" min="1" max="28" />
                        <div class="form-text small mt-0">1–28.</div>
                    </div>
                    <div class="col-4">
                        <label class="form-label mb-0 small" for="emsTime">Send At</label>
                        <input class="form-control form-control-sm" id="emsTime" type="time" />
                    </div>
                    <div class="col-12">
                        <div class="alert alert-light py-1 px-2 mb-0 small" id="emsPeriodHint"></div>
                    </div>

                    <div class="col-12"><hr class="my-1" /></div>

                    <div class="col-md-4">
                        <label class="form-label mb-0 small" for="emsCustomers">Customers</label>
                        <select class="form-select form-select-sm" id="emsCustomers" multiple size="6"></select>
                        <div class="form-text small mt-0">None selected = all customers.</div>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label mb-0 small" for="emsCommodities">Commodities</label>
                        <select class="form-select form-select-sm" id="emsCommodities" multiple size="6"></select>
                        <div class="form-text small mt-0">None selected = all commodities.</div>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label mb-0 small" for="emsSite">Location</label>
                        <select class="form-select form-select-sm" id="emsSite"></select>
                        <div class="form-text small mt-0">
                            Matched by the scale that weighed the load — tickets with
                            no scale recorded are left out.
                        </div>
                    </div>

                    <div class="col-12"><hr class="my-1" /></div>

                    <div class="col-12">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="emsGroupCommodities" />
                            <label class="form-check-label small" for="emsGroupCommodities">
                                <strong>Separate worksheet per commodity</strong>
                                <div class="form-text small mt-0">Adds a Summary sheet of per-commodity totals, then one sheet per commodity. Off puts every load on one sheet.</div>
                            </label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="emsIncludeGrossTare" checked />
                            <label class="form-check-label small" for="emsIncludeGrossTare">
                                Include Gross and Tare columns
                            </label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="emsSendWhenEmpty" />
                            <label class="form-check-label small" for="emsSendWhenEmpty">
                                Send even when nothing was weighed
                            </label>
                        </div>
                    </div>

                    <div class="col-12">
                        <label class="form-label mb-0 small" for="emsSubject">Subject <span class="text-muted">(optional)</span></label>
                        <input class="form-control form-control-sm" id="emsSubject" maxlength="200"
                               placeholder="{company} — {report} — {period}" />
                        <div class="form-text small mt-0">
                            Placeholders: <code>{company}</code> <code>{report}</code>
                            <code>{period}</code> <code>{start}</code> <code>{end}</code> <code>{count}</code>
                        </div>
                    </div>
                </div>
                <div id="emScheduleModalError" class="small text-danger mt-2"></div>
            </div>
            <div class="modal-footer py-1">
                <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary btn-sm" id="emScheduleSaveBtn">Save</button>
            </div>
        </div>
    </div>
</div>

@* Per-load email rule editor. *@
<div class="modal fade" id="emRuleModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header py-2">
                <h6 class="modal-title" id="emRuleModalTitle">Load Email Rule</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body py-2">
                <div class="row g-2">
                    <div class="col-8">
                        <label class="form-label mb-0 small" for="emrName">Rule Name</label>
                        <input class="form-control form-control-sm" id="emrName" maxlength="100" />
                    </div>
                    <div class="col-4 d-flex align-items-end">
                        <div class="form-check form-switch mb-1">
                            <input class="form-check-input" type="checkbox" id="emrActive" checked />
                            <label class="form-check-label small" for="emrActive">Active</label>
                        </div>
                    </div>
                    <div class="col-12">
                        <label class="form-label mb-0 small" for="emrRecipients">To</label>
                        <textarea class="form-control form-control-sm" id="emrRecipients" rows="2"
                                  placeholder="dispatch@customer.com, broker@example.com"></textarea>
                        <div class="form-text small mt-0">Separate addresses with commas, semicolons or new lines.</div>
                    </div>

                    <div class="col-12"><hr class="my-1" /></div>

                    <div class="col-md-6">
                        <label class="form-label mb-0 small" for="emrCustomers">Customers</label>
                        <select class="form-select form-select-sm" id="emrCustomers" multiple size="7"></select>
                        <div class="form-text small mt-0">None selected = any customer.</div>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label mb-0 small" for="emrCommodities">Commodities</label>
                        <select class="form-select form-select-sm" id="emrCommodities" multiple size="7"></select>
                        <div class="form-text small mt-0">None selected = any commodity.</div>
                    </div>

                    <div class="col-12">
                        <label class="form-label mb-0 small" for="emrSubject">Subject <span class="text-muted">(optional)</span></label>
                        <input class="form-control form-control-sm" id="emrSubject" maxlength="200"
                               placeholder="{company} — Ticket {ticket}" />
                        <div class="form-text small mt-0">
                            Placeholders: <code>{company}</code> <code>{ticket}</code>
                            <code>{customer}</code> <code>{commodity}</code> <code>{carrier}</code>
                            <code>{truck}</code> <code>{net}</code> <code>{units}</code>
                        </div>
                    </div>
                </div>
                <div id="emRuleModalError" class="small text-danger mt-2"></div>
            </div>
            <div class="modal-footer py-1">
                <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary btn-sm" id="emRuleSaveBtn">Save</button>
            </div>
        </div>
    </div>
</div>

<!-- Setup-wide notice modal (replaces native alert() calls) -->
<div class="modal fade" id="setupAlertModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content">
            <div class="modal-header py-2">
                <h6 class="modal-title">Notice</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body small" id="setupAlertBody"></div>
            <div class="modal-footer py-1">
                <button type="button" class="btn btn-primary btn-sm" data-bs-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>

<!-- Hidden forms for reset/database operations (outside main form) -->
<form asp-action="ResetTicket" method="post" id="resetTicketForm" style="display:none;">
    @Html.AntiForgeryToken()
    <input type="hidden" name="reportName" value="TicketReport" />
</form>
<form asp-action="ResetTicket" method="post" id="resetKioskTicketForm" style="display:none;">
    @Html.AntiForgeryToken()
    <input type="hidden" name="reportName" value="KioskTicketReport" />
</form>
<form asp-action="LoadTestData" method="post" id="loadTestDataForm" style="display:none;">
    @Html.AntiForgeryToken()
    <input type="hidden" name="sampleType" id="sampleTypeField" value="gravel" />
</form>

<!-- Sample-data picker (Load Test Data) -->
<div class="modal fade" id="sampleDataModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header py-2">
                <h6 class="modal-title">Load Test Data</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body py-2">
                <p class="small mb-2">Pick a sample business. This <strong>deletes all existing data</strong> and loads about a year of tickets plus matching master data.</p>
                <div class="form-check mb-1">
                    <input class="form-check-input" type="radio" name="sampleTypeRadio" id="sampleGravel" value="gravel" checked />
                    <label class="form-check-label" for="sampleGravel">
                        <strong>Gravel Pit / Quarry</strong>
                        <div class="form-text small mt-0">Aggregate commodities, contractor customers, job-site destinations. Bin Inventory off.</div>
                    </label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="sampleTypeRadio" id="sampleFarming" value="farming" />
                    <label class="form-check-label" for="sampleFarming">
                        <strong>Farming / Grain</strong>
                        <div class="form-text small mt-0">Corn, soybeans, and wheat hauled from fields into 6 bins at harvest, sold out to grain buyers, with shrinkage true-ups. Bin Inventory on.</div>
                    </label>
                </div>
            </div>
            <div class="modal-footer py-1">
                <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-warning btn-sm" id="sampleDataLoadBtn">Delete Everything &amp; Load Sample</button>
            </div>
        </div>
    </div>
</div>
<form asp-action="ClearTransactions" method="post" id="clearTransactionsForm" style="display:none;">
    @Html.AntiForgeryToken()
</form>
<form asp-action="ClearDatabase" method="post" id="clearDatabaseForm" style="display:none;">
    @Html.AntiForgeryToken()
</form>

<div class="modal fade" id="confirmModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header py-2">
                <h6 class="modal-title" id="confirmModalTitle">Confirm Operation</h6>
                <button type="button" class="btn-close btn-close-sm" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body py-3" id="confirmModalBody"></div>
            <div class="modal-footer py-2">
                <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-sm" id="confirmModalBtn" onclick="confirmModalSubmit()">Confirm</button>
            </div>
        </div>
    </div>
</div>

<!-- Custom Field editor modal (add + edit) -->
<div class="modal fade" id="customFieldModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header py-2">
                <h6 class="modal-title" id="cfModalTitle">Add Custom Field</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body py-2">
                <div class="mb-2">
                    <label class="form-label mb-0 small">Field Name</label>
                    <input type="text" id="cfName" class="form-control form-control-sm" maxlength="50" />
                </div>
                <div class="row g-2 mb-2">
                    <div class="col-4">
                        <label class="form-label mb-0 small">Type</label>
                        <select id="cfType" class="form-select form-select-sm">
                            <option value="Text">Text</option>
                            <option value="Integer">Integer</option>
                            <option value="Real">Decimal</option>
                            <option value="Formula">Formula</option>
                        </select>
                    </div>
                    <div class="col-4">
                        <label class="form-label mb-0 small">Entry</label>
                        <select id="cfRequired" class="form-select form-select-sm">
                            <option value="false">Optional</option>
                            <option value="true">Required</option>
                        </select>
                    </div>
                    <div class="col-4">
                        <label class="form-label mb-0 small">Sort Order</label>
                        <input type="number" id="cfSortOrder" class="form-control form-control-sm" value="0" step="1" />
                    </div>
                </div>
                <div class="mb-2" id="cfParentSection" style="display:none;">
                    <label class="form-label mb-0 small">Sub-field of <span class="text-muted">(optional — makes this a cascading dropdown)</span></label>
                    <select id="cfParentField" class="form-select form-select-sm"></select>
                    <div class="form-text small">
                        The choices are grouped under each value of the parent — e.g. a
                        <em>Variety</em> field under Commodity, then a <em>Grade</em> field
                        under Variety. The forms and kiosk only offer the choices that
                        belong to the selected parent value.
                    </div>
                </div>
                <div class="mb-2" id="cfListSection" style="display:none;">
                    <label class="form-label mb-0 small">Dropdown values <span class="text-muted">(optional — one per line; leave empty for free text)</span></label>
                    <textarea id="cfListValues" class="form-control form-control-sm" rows="5"
                              placeholder="Value 1&#10;Value 2&#10;Value 3"></textarea>
                    <div class="form-text small">Add, change, delete, or reorder lines to manage the list. Values already saved on tickets are kept even if removed here.</div>
                </div>
                <div class="mb-2" id="cfParentValuesHint" style="display:none;">
                    <div class="alert alert-info py-1 mb-0 small">
                        <i class="fa fa-circle-info"></i>
                        Manage this field's choices per parent value on
                        <a href="/MasterData">Tables</a> after saving — the field gets
                        its own tab there.
                    </div>
                </div>
                <div class="mb-2" id="cfFormulaSection" style="display:none;">
                    <label class="form-label mb-0 small">Formula</label>
                    <input type="text" id="cfFormula" class="form-control form-control-sm" maxlength="500"
                           placeholder="e.g. NetWeight / 56  or  [Price] * NetTons" />
                    <div class="form-text small">
                        Computed automatically whenever the ticket is saved and shown in
                        grids, reports, and printed tickets. Reference
                        <code>NetWeight</code>, <code>GrossWeight</code>, <code>TareWeight</code>,
                        <code>InWeight</code>, <code>OutWeight</code>, <code>NetTons</code>,
                        or any numeric custom field by name — use <code>[brackets]</code>
                        when the name has spaces. Operators <code>+ - * / ( )</code> and
                        functions <code>round(x, n)</code>, <code>abs</code>, <code>min</code>,
                        <code>max</code>. A missing referenced value leaves the result blank.
                    </div>
                </div>
                <div class="row g-2 mb-2" id="cfNumberSection" style="display:none;">
                    <div class="col-4" id="cfMinCol">
                        <label class="form-label mb-0 small">Min <span class="text-muted">(optional)</span></label>
                        <input type="number" id="cfMinValue" class="form-control form-control-sm" step="any" />
                    </div>
                    <div class="col-4" id="cfMaxCol">
                        <label class="form-label mb-0 small">Max <span class="text-muted">(optional)</span></label>
                        <input type="number" id="cfMaxValue" class="form-control form-control-sm" step="any" />
                    </div>
                    <div class="col-4" id="cfPrecisionCol">
                        <label class="form-label mb-0 small">Decimals <span class="text-muted">(0–6)</span></label>
                        <input type="number" id="cfPrecision" class="form-control form-control-sm" min="0" max="6" step="1" />
                    </div>
                </div>
                <div class="d-flex gap-4 flex-wrap">
                    <div class="form-check"
                         title="Checked: the value prints automatically on tickets. Unchecked: it only prints if you place the field's cf_ parameter in the Ticket Designer.">
                        <input type="checkbox" id="cfShowOnTicket" class="form-check-input" checked />
                        <label class="form-check-label small" for="cfShowOnTicket">Show on printed ticket</label>
                    </div>
                    <div class="form-check">
                        <input type="checkbox" id="cfActive" class="form-check-input" checked />
                        <label class="form-check-label small" for="cfActive">Active</label>
                    </div>
                    <div class="form-check" title="Numbers and dropdown lists only — free text can't prompt at the kiosk">
                        <input type="checkbox" id="cfPromptAtKiosk" class="form-check-input" />
                        <label class="form-check-label small" for="cfPromptAtKiosk">Ask at kiosk (weigh-in)</label>
                    </div>
                </div>
                <div id="cfModalError" class="small text-danger mt-2" style="display:none;"></div>
            </div>
            <div class="modal-footer py-1">
                <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary btn-sm" id="cfSaveBtn">Save Field</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script src="~/js/signalr/signalr.min.js"></script>
<script>
    // --- Bootstrap notice helper (replaces window.alert for in-form messages) ---
    function showSetupAlert(message) {
        document.getElementById('setupAlertBody').textContent = message;
        var modalEl = document.getElementById('setupAlertModal');
        var modal = bootstrap.Modal.getInstance(modalEl) || new bootstrap.Modal(modalEl);
        modal.show();
    }

    // --- Tab persistence: remember which tab is active and re-activate after save ---
    (function () {
        // A ?tab= query wins (used by the theme-change reload); otherwise the
        // server-provided value (TempData after a classic post) or the default.
        var queryTab = new URLSearchParams(window.location.search).get('tab');
        if (queryTab && !/^[A-Za-z0-9_-]+$/.test(queryTab)) queryTab = null;
        if (queryTab && document.querySelector('[data-bs-target="#' + queryTab + '"]')) {
            document.getElementById('activeTabField').value = queryTab;
        }
        var initial = document.getElementById('activeTabField').value || 'tabCompany';
        var btn = document.querySelector('[data-bs-target="#' + initial + '"]');
        if (btn && !btn.classList.contains('active')) {
            new bootstrap.Tab(btn).show();
        }
        // When the user clicks a tab, record it so the form post carries it.
        document.querySelectorAll('[data-bs-toggle="tab"]').forEach(function (b) {
            b.addEventListener('shown.bs.tab', function (e) {
                var target = (e.target.getAttribute('data-bs-target') || '').replace('#', '');
                if (target) document.getElementById('activeTabField').value = target;
            });
        });
    })();

    // --- Services tab: what is connected, and what version it is running ---
    // Loaded on demand rather than with the page: it is a live read of hub
    // connection state, so a value fetched at page load would be stale by the
    // time anyone clicked through to look at it.
    (function () {
        var body = document.getElementById('servicesTableBody');
        if (!body) return;

        function escapeHtml(s) {
            return String(s).replace(/[&<>"']/g, function (c) {
                return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
            });
        }

        function statusCell(svc) {
            if (svc.version === null || svc.version === undefined) {
                // Connected but silent. Almost always a service predating the
                // version handshake, so say what to do rather than just "?".
                return '<span class="badge bg-secondary">Not reported</span>' +
                    '<div class="small text-muted">Update it to report its version</div>';
            }
            if (!svc.releasedWithServer) {
                // Its own product, on its own release line. Comparing it to this
                // server's version would mark it wrong forever, so show the
                // version and say plainly where to check it instead.
                return '<span class="text-muted small">Released separately —' +
                    ' check against its own release</span>';
            }
            // "Does not match", not "older": a service can also be ahead of the
            // server mid-rollout, and calling that out of date would send someone
            // to update the wrong machine.
            return svc.upToDate
                ? '<span class="badge bg-success">Up to date</span>'
                : '<span class="badge bg-warning text-dark">Does not match server</span>';
        }

        function load() {
            body.innerHTML = '<tr><td colspan="4" class="text-muted small">Loading…</td></tr>';
            fetch('/api/setup/service-versions')
                .then(function (r) { return r.json(); })
                .then(function (data) {
                    document.getElementById('serverVersion').textContent = data.server;
                    if (!data.services.length) {
                        body.innerHTML = '<tr><td colspan="4" class="text-muted small">' +
                            'Nothing connected. Scale readers, print services, card readers, ' +
                            'cameras and the QuickBooks sync appear here while they are running.' +
                            '</td></tr>';
                        return;
                    }
                    body.innerHTML = data.services.map(function (s) {
                        return '<tr>' +
                            '<td>' + escapeHtml(s.kind) + '</td>' +
                            '<td><code class="small">' + escapeHtml(s.serviceId) + '</code></td>' +
                            '<td>' + (s.version ? escapeHtml(s.version) : '<span class="text-muted">—</span>') + '</td>' +
                            '<td>' + statusCell(s) + '</td>' +
                            '</tr>';
                    }).join('');
                })
                .catch(function () {
                    body.innerHTML = '<tr><td colspan="4" class="text-danger small">' +
                        'Could not read service versions from the server.</td></tr>';
                });
        }

        document.getElementById('btnRefreshServices').addEventListener('click', load);
        var tabBtn = document.querySelector('[data-bs-target="#tabServices"]');
        if (tabBtn) tabBtn.addEventListener('shown.bs.tab', load);
        // The tab can also be the one restored after a save, in which case no
        // click ever fires.
        if (document.querySelector('#tabServices.active')) load();
    })();

    // --- Auto-save: every change posts the whole form (no Save button) ---
    // Saves fire on 'change' (blur for text inputs, immediate for selects,
    // checkboxes, and radios) so a half-typed Ticket Number or PIN is never
    // saved mid-keystroke. A short debounce coalesces the cascades from the
    // rule-sync scripts (e.g. Retained Tare forcing several controls at once).
    var setupForm = document.getElementById('setupForm');
    var saveStatus = document.getElementById('saveStatus');
    var saveTimer = null;
    var savePending = false;
    var saveStatusTimer = null;
    // Theme only takes effect when the page re-renders, so a theme change
    // reloads the page after its save lands (back onto the same tab).
    var reloadAfterSave = false;

    function showSaveStatus(text, isError) {
        clearTimeout(saveStatusTimer);
        saveStatus.textContent = text;
        saveStatus.style.display = 'block';
        saveStatus.style.color = isError ? '#dc3545' : 'var(--bw-text)';
        if (!isError && text !== 'Saving…') {
            saveStatusTimer = setTimeout(function () { saveStatus.style.display = 'none'; }, 2000);
        }
    }

    function autoSave() {
        if (savePending) { scheduleAutoSave(); return; } // serialize overlapping saves
        savePending = true;
        showSaveStatus('Saving…', false);
        var hadIconFile = document.getElementById('iconFileInput').files.length > 0;
        var removeCbEl = document.getElementById('removeIconCb');
        fetch(setupForm.action || window.location.pathname, {
            method: 'POST',
            body: new FormData(setupForm),
            headers: { 'X-Requested-With': 'XMLHttpRequest' }
        }).then(function (r) {
            if (!r.ok) throw new Error('HTTP ' + r.status);
            // Don't re-send one-shot icon operations on the next save.
            if (hadIconFile) document.getElementById('iconFileInput').value = '';
            if (removeCbEl && removeCbEl.checked) removeCbEl.checked = false;
            if (reloadAfterSave) {
                showSaveStatus('Saved — applying theme…', false);
                var tab = document.getElementById('activeTabField').value || 'tabCompany';
                window.location.replace(window.location.pathname + '?tab=' + encodeURIComponent(tab));
                return;
            }
            showSaveStatus('All changes saved ✓', false);
        }).catch(function () {
            showSaveStatus('Save failed — check connection and change a setting to retry', true);
        }).finally(function () {
            savePending = false;
        });
    }

    function scheduleAutoSave() {
        clearTimeout(saveTimer);
        saveTimer = setTimeout(autoSave, 400);
    }

    setupForm.addEventListener('change', function (e) {
        if (!e.target || !e.target.matches('input, select, textarea')) return;
        // The Locations and Email editors save through their own APIs — don't
        // also post the whole settings form for those edits.
        if (e.target.closest('#locationsEditor')) return;
        if (e.target.closest('#emailEditor')) return;
        if (e.target.id === 'Theme') reloadAfterSave = true;
        scheduleAutoSave();
    });

    // --- Icon preview ---
    document.getElementById('iconFileInput').addEventListener('change', function (e) {
        var file = e.target.files[0];
        if (file) {
            var reader = new FileReader();
            reader.onload = function (ev) {
                document.getElementById('iconPreview').src = ev.target.result;
            };
            reader.readAsDataURL(file);
            var cb = document.getElementById('removeIconCb');
            if (cb) cb.checked = false;
        }
    });

    var removeCb = document.getElementById('removeIconCb');
    if (removeCb) {
        removeCb.addEventListener('change', function () {
            var preview = document.getElementById('iconPreview');
            if (this.checked) {
                preview.dataset.originalSrc = preview.src;
                preview.src = '/images/default-icon.svg';
            } else {
                preview.src = preview.dataset.originalSrc || '/api/setup/icon';
            }
        });
    }

    // Show or hide one of the button cells that sit beside a Yes/No select.
    //
    // Those cells carry Bootstrap's .d-flex, which is `display: flex !important`,
    // so a plain el.style.display = 'none' loses to it and the button stays on
    // screen after its feature is switched off. Razor gets the first render
    // right by writing !important inline; this keeps the runtime toggles right
    // too. Only needed for .d-flex cells — the plain block ones are fine as they
    // are.
    function setSectionDisplay(el, value) {
        if (el) el.style.setProperty('display', value, 'important');
    }

    // --- QB Service Check ---
    document.getElementById('UseQuickBooks').addEventListener('change', function () {
        var el = document.getElementById('qbCheckSection');
        setSectionDisplay(el, this.value === 'true' ? 'flex' : 'none');
    });

    document.getElementById('SavePicture').addEventListener('change', function () {
        var el = document.getElementById('cameraManageSection');
        setSectionDisplay(el, this.value === 'true' ? 'flex' : 'none');
    });

    // --- Card options ---
    document.getElementById('UseCardReader').addEventListener('change', function () {
        var on = this.value === 'true';
        document.getElementById('recycleCardsSection').style.display = on ? 'block' : 'none';
        setSectionDisplay(document.getElementById('cardManageSection'), on ? 'flex' : 'none');
        setSectionDisplay(document.getElementById('readerManageSection'), on ? 'flex' : 'none');
        document.getElementById('cardSurfaceGates').style.display = on ? 'flex' : 'none';
    });

    // --- Driver Signature options ---
    function syncSignatureSections() {
        var mode = document.getElementById('SignatureMode').value;
        document.querySelectorAll('.signature-option').forEach(function (el) {
            el.style.display = mode !== 'None' ? 'block' : 'none';
        });
        document.getElementById('signaturePadIdSection').style.display = mode === 'RemotePad' ? 'flex' : 'none';
        var padId = document.getElementById('SignaturePadId').value || 'default';
        var padUrl = window.location.origin + '/SignaturePad?pad-id=' + encodeURIComponent(padId);
        document.getElementById('signaturePadUrl').textContent = padUrl;
        document.getElementById('signaturePadLink').href = padUrl;
        // QR of the same URL so the tablet can scan instead of typing.
        document.getElementById('signaturePadQr').src =
            '/api/setup/signature-pad-qr?padId=' + encodeURIComponent(padId);
    }
    document.getElementById('SignatureMode').addEventListener('change', syncSignatureSections);
    document.getElementById('SignaturePadId').addEventListener('input', syncSignatureSections);
    syncSignatureSections();

    // --- Standard field visibility rules (mirror the server-side forcing) ---
    (function () {
        var carrier = document.getElementById('HideCarrier');
        var truck = document.getElementById('HideTruckId');
        var retained = document.getElementById('UseRetainedTare');

        function syncVisibilityRules() {
            var rtOn = retained && retained.value === 'true';
            if (rtOn) {
                carrier.value = 'false';
                truck.value = 'false';
            }
            carrier.disabled = rtOn;
            if (carrier.value === 'true') truck.value = 'true';
            truck.disabled = rtOn || carrier.value === 'true';
        }
        carrier.addEventListener('change', syncVisibilityRules);
        truck.addEventListener('change', syncVisibilityRules);
        if (retained) retained.addEventListener('change', syncVisibilityRules);
        syncVisibilityRules();
    })();

    // --- Custom Fields CRUD (saves immediately via /api/customfields) ---
    (function () {
        var editingId = null;
        var loadedFields = [];

        // Default a NEW field's order to the end of the shared scale: after
        // every existing custom field and after the standard fields' orders.
        function nextSortOrder() {
            var max = 0;
            loadedFields.forEach(function (f) { if (f.sortOrder > max) max = f.sortOrder; });
            document.querySelectorAll('input[name^="FieldOrder"]').forEach(function (el) {
                var v = parseInt(el.value) || 0;
                if (v > max) max = v;
            });
            return max + 10;
        }

        function esc(s) {
            return String(s ?? '').replace(/[&<>"']/g, function (c) {
                return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
            });
        }

        function parentLabel(parentField) {
            if (!parentField) return null;
            if (parentField.indexOf('cf_') === 0) {
                var pid = parseInt(parentField.substring(3));
                var p = loadedFields.find(function (f) { return f.id === pid; });
                return p ? p.name : parentField;
            }
            return parentField === 'TruckId' ? 'Truck ID' : parentField;
        }

        function typeLabel(f) {
            var t = f.fieldType === 'Real' ? 'Decimal' : f.fieldType;
            if (f.fieldType === 'Text' && f.parentField) t += ' (List under ' + esc(parentLabel(f.parentField)) + ')';
            else if (f.fieldType === 'Text' && f.listValues) t += ' (List)';
            else if (f.fieldType === 'Formula' && f.formula) t += ': ' + esc(f.formula);
            return t;
        }

        // Show the dropdown-list editor for Text, the min/max (+ decimals for
        // Decimal) inputs for number types. The kiosk checkbox only applies to
        // constrained inputs: numbers, or Text with a non-empty dropdown list.
        // A sub-field (parent set) manages its choices per parent value on
        // Edit Tables, so the flat textarea is hidden for it.
        function syncCfTypeSections() {
            var t = document.getElementById('cfType').value;
            var isFormula = t === 'Formula';
            var hasParent = document.getElementById('cfParentField').value !== '';
            document.getElementById('cfParentSection').style.display = t === 'Text' ? 'block' : 'none';
            document.getElementById('cfListSection').style.display = (t === 'Text' && !hasParent) ? 'block' : 'none';
            document.getElementById('cfParentValuesHint').style.display = (t === 'Text' && hasParent) ? 'block' : 'none';
            document.getElementById('cfFormulaSection').style.display = isFormula ? 'block' : 'none';
            // Formula: only Decimals applies (no min/max, nothing typed in).
            document.getElementById('cfNumberSection').style.display = t === 'Text' ? 'none' : 'flex';
            document.getElementById('cfMinCol').style.display = isFormula ? 'none' : 'block';
            document.getElementById('cfMaxCol').style.display = isFormula ? 'none' : 'block';
            document.getElementById('cfPrecisionCol').style.display = (t === 'Real' || isFormula) ? 'block' : 'none';
            var req = document.getElementById('cfRequired');
            req.disabled = isFormula;
            if (isFormula) req.value = 'false';
            var kiosk = document.getElementById('cfPromptAtKiosk');
            var eligible = !isFormula && (t !== 'Text' || hasParent
                || document.getElementById('cfListValues').value.trim() !== '');
            kiosk.disabled = !eligible;
            if (!eligible) kiosk.checked = false;
        }
        document.getElementById('cfType').addEventListener('change', syncCfTypeSections);
        document.getElementById('cfListValues').addEventListener('input', syncCfTypeSections);
        document.getElementById('cfParentField').addEventListener('change', syncCfTypeSections);

        // Parent choices: the standard dropdown-backed fields plus every OTHER
        // list-backed text custom field. Self is excluded; deeper loops are
        // rejected server-side.
        function fillCfParentOptions(currentParent, selfId) {
            var sel = document.getElementById('cfParentField');
            sel.innerHTML = '';
            sel.appendChild(new Option('— None (top-level field) —', ''));
            var standard = ['Commodity', 'Customer', 'Carrier', 'Location', 'Destination'];
            @if (Model.UseBinInventory)
            {
                <text>standard.push('Bin');</text>
            }
            standard.forEach(function (name) { sel.appendChild(new Option(name, name)); });
            loadedFields.forEach(function (f) {
                if (f.fieldType !== 'Text') return;
                if (selfId && f.id === selfId) return;
                if (!f.listValues && !f.parentField) return; // free text can't be a parent
                sel.appendChild(new Option(f.name + ' (custom field)', 'cf_' + f.id));
            });
            // Keep a stored parent selectable even if it no longer qualifies.
            if (currentParent && !Array.from(sel.options).some(function (o) { return o.value === currentParent; })) {
                sel.appendChild(new Option(parentLabel(currentParent) + ' (retired)', currentParent));
            }
            sel.value = currentParent || '';
        }

        function loadCustomFields() {
            fetch('/api/customfields').then(function (r) { return r.json(); }).then(function (fields) {
                loadedFields = fields;
                var tbody = document.querySelector('#customFieldsTable tbody');
                tbody.innerHTML = '';
                document.getElementById('customFieldsEmpty').style.display = fields.length ? 'none' : 'block';
                fields.forEach(function (f) {
                    var tr = document.createElement('tr');
                    if (!f.active) tr.style.opacity = '0.5';
                    tr.innerHTML =
                        '<td class="fw-bold">' + esc(f.name) + '</td>' +
                        '<td>' + typeLabel(f) + '</td>' +
                        '<td>' + (f.required ? '<span class="badge bg-danger">Required</span>' : 'Optional') + '</td>' +
                        '<td>' + (f.showOnTicket ? 'Yes' : 'No') + '</td>' +
                        '<td>' + f.sortOrder + '</td>' +
                        '<td>' + (f.active ? 'Yes' : 'No') + '</td>' +
                        '<td class="text-end text-nowrap">' +
                        '<button type="button" class="btn btn-outline-secondary btn-sm py-0 me-1 cf-edit">Edit</button>' +
                        '<button type="button" class="btn btn-outline-danger btn-sm py-0 cf-delete">Delete</button></td>';
                    tr.querySelector('.cf-edit').addEventListener('click', function () { openModal(f); });
                    tr.querySelector('.cf-delete').addEventListener('click', function () { deleteField(f); });
                    tbody.appendChild(tr);
                });
            });
        }

        function openModal(field) {
            editingId = field ? field.id : null;
            document.getElementById('cfModalTitle').textContent = field ? 'Edit Custom Field' : 'Add Custom Field';
            document.getElementById('cfName').value = field ? field.name : '';
            document.getElementById('cfType').value = field ? field.fieldType : 'Text';
            document.getElementById('cfRequired').value = field && field.required ? 'true' : 'false';
            document.getElementById('cfSortOrder').value = field ? field.sortOrder : nextSortOrder();
            document.getElementById('cfShowOnTicket').checked = field ? field.showOnTicket : true;
            document.getElementById('cfActive').checked = field ? field.active : true;
            document.getElementById('cfListValues').value = field && field.listValues ? field.listValues : '';
            document.getElementById('cfFormula').value = field && field.formula ? field.formula : '';
            fillCfParentOptions(field ? field.parentField : null, field ? field.id : null);
            document.getElementById('cfMinValue').value = field && field.minValue != null ? field.minValue : '';
            document.getElementById('cfMaxValue').value = field && field.maxValue != null ? field.maxValue : '';
            document.getElementById('cfPrecision').value = field && field.precision != null ? field.precision : '';
            document.getElementById('cfPromptAtKiosk').checked = field ? !!field.promptAtKiosk : false;
            document.getElementById('cfModalError').style.display = 'none';
            syncCfTypeSections();
            new bootstrap.Modal('#customFieldModal').show();
        }

        function deleteField(f) {
            if (!confirm('Delete "' + f.name + '"? Values already saved on tickets will be removed. ' +
                         'To keep history, set the field to inactive instead.')) return;
            fetch('/api/customfields/' + f.id, { method: 'DELETE' }).then(loadCustomFields);
        }

        document.getElementById('addCustomFieldBtn').addEventListener('click', function () { openModal(null); });

        document.getElementById('cfSaveBtn').addEventListener('click', function () {
            var body = {
                id: editingId || 0,
                name: document.getElementById('cfName').value.trim(),
                fieldType: document.getElementById('cfType').value,
                required: document.getElementById('cfRequired').value === 'true',
                sortOrder: parseInt(document.getElementById('cfSortOrder').value) || 0,
                showOnTicket: document.getElementById('cfShowOnTicket').checked,
                active: document.getElementById('cfActive').checked,
                listValues: document.getElementById('cfListValues').value || null,
                parentField: document.getElementById('cfParentField').value || null,
                formula: document.getElementById('cfFormula').value.trim() || null,
                minValue: document.getElementById('cfMinValue').value === '' ? null : parseFloat(document.getElementById('cfMinValue').value),
                maxValue: document.getElementById('cfMaxValue').value === '' ? null : parseFloat(document.getElementById('cfMaxValue').value),
                precision: document.getElementById('cfPrecision').value === '' ? null : parseInt(document.getElementById('cfPrecision').value),
                promptAtKiosk: document.getElementById('cfPromptAtKiosk').checked
            };
            fetch(editingId ? '/api/customfields/' + editingId : '/api/customfields', {
                method: editingId ? 'PUT' : 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(body)
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Save failed.'); });
                bootstrap.Modal.getInstance(document.getElementById('customFieldModal')).hide();
                loadCustomFields();
            }).catch(function (err) {
                var el = document.getElementById('cfModalError');
                el.textContent = err.message;
                el.style.display = 'block';
            });
        });

        loadCustomFields();
    })();

    // Single source of truth for whether each "Allow Skip" checkbox is enabled.
    // A prompt set to Neither will never fire, so its skip flag is meaningless;
    // unchecked + disabled. Carrier/Truck additionally have skip locked off
    // whenever Retained Tare is on (handled here too).
    var SKIP_RULES = [
        { group: 'kioskCarrierDir',   skip: 'AllowSkipCarrier',     rtForcesOff: true  },
        { group: 'kioskTruckDir',     skip: 'AllowSkipTruckId',     rtForcesOff: true  },
        { group: 'kioskCommodityDir', skip: 'AllowSkipCommodity',   rtForcesOff: false },
        { group: 'kioskCustomerDir',  skip: 'AllowSkipCustomer',    rtForcesOff: false },
        { group: 'kioskLocationDir',  skip: 'AllowSkipLocation',    rtForcesOff: false },
        { group: 'kioskDestDir',      skip: 'AllowSkipDestination', rtForcesOff: false },
        { group: 'kioskBinDir',       skip: 'AllowSkipBin',         rtForcesOff: false }
    ];
    function syncAllowSkipState() {
        var rtOn = isRetainedTareOn();
        var binRequiredSel = document.getElementById('BinRequired');
        var binRequired = binRequiredSel && binRequiredSel.value === 'true';
        SKIP_RULES.forEach(function (row) {
            var skip = document.getElementById(row.skip);
            if (!skip) return;
            var checkedRadio = document.querySelector('input[name="' + row.group + '"]:checked');
            var dir = checkedRadio ? checkedRadio.value : 'neither';
            var force = dir === 'neither' || (row.rtForcesOff && rtOn)
                || (row.group === 'kioskBinDir' && binRequired);
            if (force) {
                skip.checked = false;
                skip.disabled = true;
            } else {
                skip.disabled = false;
            }
        });
    }

    function applyRetainedTareLocks(on) {
        // Visual lock + force values for Carrier/Truck while Retained Tare is on:
        //   - Direction radios pinned to Inbound (Neither disabled too)
        //   - AllowSkip — handled by syncAllowSkipState below
        //   - "required" badges + tab notice visible
        // For the splittable prompts (Commodity / Customer / Location /
        // Destination), the Outbound radio is disabled — the auto-completed
        // weigh-out under Retained Tare never fires those prompts. Any
        // Outbound selection is bumped to Inbound so the prompt still happens
        // somewhere reasonable.
        if (on) {
            var carrierInbound = document.getElementById('carrierDirInbound');
            var truckInbound   = document.getElementById('truckDirInbound');
            if (carrierInbound && !carrierInbound.checked) { carrierInbound.checked = true; carrierInbound.dispatchEvent(new Event('change')); }
            if (truckInbound && !truckInbound.checked)     { truckInbound.checked = true;   truckInbound.dispatchEvent(new Event('change')); }
        }
        // Disable the "Neither" radio so it can't even be clicked while RT is on.
        ['carrierDirNone', 'truckDirNone'].forEach(function (id) {
            var el = document.getElementById(id);
            if (el) el.disabled = on;
        });
        // Outbound radios for splittable prompts: disable while RT is on; if
        // currently checked, push selection to Inbound.
        ['kioskCommodityDirOutbound', 'kioskCustomerDirOutbound', 'kioskLocationDirOutbound', 'kioskDestDirOutbound', 'kioskBinDirOutbound'].forEach(function (id) {
            var outRadio = document.getElementById(id);
            if (!outRadio) return;
            outRadio.disabled = on;
            if (on && outRadio.checked) {
                var inboundId = id.replace(/Outbound$/, 'Inbound');
                var inboundRadio = document.getElementById(inboundId);
                if (inboundRadio) { inboundRadio.checked = true; inboundRadio.dispatchEvent(new Event('change')); }
            }
        });
        ['PromptKioskCarrierLock', 'PromptKioskTruckIdLock'].forEach(function (id) {
            var el = document.getElementById(id);
            if (el) el.style.display = on ? 'inline' : 'none';
        });
        var notice = document.getElementById('retainedTareRequiresPrompts');
        if (notice) notice.style.display = on ? 'block' : 'none';
        syncAllowSkipState();
    }

    document.getElementById('UseRetainedTare').addEventListener('change', function () {
        var on = this.value === 'true';
        setSectionDisplay(document.getElementById('retainedTareManageSection'), on ? 'flex' : 'none');
        var autoClear = document.getElementById('autoClearStaleSection');
        if (autoClear) autoClear.style.display = on ? 'block' : 'none';
        var resetGates = document.getElementById('tareResetGates');
        if (resetGates) resetGates.style.display = on ? 'flex' : 'none';
        applyRetainedTareLocks(on);
    });
    // Apply on initial load too — covers the case where the page renders with
    // Retained Tare already enabled.
    applyRetainedTareLocks(document.getElementById('UseRetainedTare').value === 'true');

    function isRetainedTareOn() {
        var sel = document.getElementById('UseRetainedTare');
        return sel && sel.value === 'true';
    }
    // Defense in depth: if a stale cached page somehow lets a user pick Neither
    // on Carrier/Truck, snap back and surface a Bootstrap notice.
    [['kioskCarrierDir', 'carrierDirInbound'], ['kioskTruckDir', 'truckDirInbound']].forEach(function (pair) {
        document.querySelectorAll('input[name="' + pair[0] + '"]').forEach(function (r) {
            r.addEventListener('change', function () {
                if (this.value === 'neither' && isRetainedTareOn()) {
                    var inboundRadio = document.getElementById(pair[1]);
                    if (inboundRadio) { inboundRadio.checked = true; inboundRadio.dispatchEvent(new Event('change')); }
                    showSetupAlert('Carrier and Truck ID prompts are required while Retained Tare is enabled. Turn off Retained Tare first to disable them.');
                }
            });
        });
    });

    document.getElementById('RemotePrintMode').addEventListener('change', function () {
        var el = document.getElementById('printerManageSection');
        setSectionDisplay(el, this.value === 'RemotePrinter' ? 'flex' : 'none');
    });

    function checkQBService() {
        var btn = document.getElementById('btnCheckQBService');
        btn.disabled = true;
        btn.innerHTML = '<i class="fa fa-spinner fa-spin"></i>';
        var conn = new signalR.HubConnectionBuilder().withUrl('/scaleHub').build();
        conn.start().then(function () {
            return conn.invoke('CheckQBServiceConnected');
        }).then(function (connected) {
            conn.stop();
            btn.disabled = false;
            btn.innerHTML = '<i class="fa fa-plug"></i> Check';
            var container = document.getElementById('qb-status-alert');
            if (!container) {
                container = document.createElement('div');
                container.id = 'qb-status-alert';
                btn.parentElement.parentElement.after(container);
            }
            if (connected) {
                container.innerHTML = '<div class="alert alert-success alert-dismissible fade show py-1 px-2 small mt-1" role="alert">' +
                    '<i class="fa fa-check-circle"></i> QuickBooks Sync Service is connected.' +
                    '<button type="button" class="btn-close btn-close-sm p-1" data-bs-dismiss="alert"></button></div>';
            } else {
                container.innerHTML = '<div class="alert alert-danger alert-dismissible fade show py-1 px-2 small mt-1" role="alert">' +
                    '<i class="fa fa-times-circle"></i> QuickBooks Sync Service is not connected. Please start the QBSyncService.' +
                    '<button type="button" class="btn-close btn-close-sm p-1" data-bs-dismiss="alert"></button></div>';
            }
        }).catch(function (err) {
            conn.stop();
            btn.disabled = false;
            btn.innerHTML = '<i class="fa fa-plug"></i> Check';
        });
    }

    // (Scale assignment moved to the Scales page — named site scales.)

    // --- Dynamic visibility toggles ---
    // The default-language picker is meaningless while Spanish is off.
    document.getElementById('EnableSpanish').addEventListener('change', function () {
        document.getElementById('languageSection').style.display = this.checked ? 'block' : 'none';
    });

    document.getElementById('UseLogin').addEventListener('change', function () {
        var useLogin = this.value === 'true';
        document.getElementById('kioskCodeSection').style.display = useLogin ? 'block' : 'none';
        document.getElementById('tabSecurityNav').style.display = useLogin ? 'block' : 'none';
    });

    var kioskCodeInput = document.querySelector('input[name="KioskCode"]');
    if (kioskCodeInput) {
        kioskCodeInput.addEventListener('input', function () {
            var pin = this.value || '12345';
            var pinDisplay = document.getElementById('kioskPinDisplay');
            var pinUrl = document.getElementById('kioskPinUrl');
            if (pinDisplay) pinDisplay.textContent = pin;
            if (pinUrl) pinUrl.textContent = pin;
        });
    }

    // API Definition PIN live update
    var apiPinInput = document.getElementById('ApiDefinitionPin');
    if (apiPinInput) {
        apiPinInput.addEventListener('input', function () {
            var pin = this.value || '12345';
            document.getElementById('apiPinUrl').textContent = pin;
            document.getElementById('swaggerLink').href = '/swagger?pin=' + encodeURIComponent(pin);
        });
    }

    document.getElementById('KioskCount').addEventListener('change', function () {
        var hasKiosk = parseInt(this.value) > 0;
        document.getElementById('tabKioskPromptsNav').style.display = hasKiosk ? 'block' : 'none';
        document.getElementById('kioskDarkModeSection').style.display = hasKiosk ? 'block' : 'none';
    });

    // ===== Kiosk Prompt radios =====
    // Carrier and Truck ID radios are 2-state (Neither / Inbound) — they only
    // gate a single underlying bool, so map directly to the hidden input.
    function bindBoolDirectionRadio(groupName, hiddenId) {
        document.querySelectorAll('input[name="' + groupName + '"]').forEach(function (radio) {
            radio.addEventListener('change', function () {
                document.getElementById(hiddenId).value = (this.value === 'inbound').toString().toLowerCase();
                if (groupName === 'kioskCarrierDir') {
                    // Truck ID row visibility tracks the carrier prompt.
                    document.getElementById('truckIdPromptSection').style.display =
                        this.value === 'inbound' ? 'table-row' : 'none';
                }
                syncAllowSkipState();
            });
        });
    }
    bindBoolDirectionRadio('kioskCarrierDir', 'PromptKioskCarrier');
    bindBoolDirectionRadio('kioskTruckDir',   'PromptKioskTruckId');

    // Splittable prompts (Commodity, Customer, Location, Destination) write
    // separate OnInbound / OnOutbound hidden bools.
    function bindSplitDirectionRadio(groupName, inHiddenId, outHiddenId) {
        document.querySelectorAll('input[name="' + groupName + '"]').forEach(function (radio) {
            radio.addEventListener('change', function () {
                document.getElementById(inHiddenId).value  = (this.value === 'inbound').toString().toLowerCase();
                document.getElementById(outHiddenId).value = (this.value === 'outbound').toString().toLowerCase();
                syncAllowSkipState();
            });
        });
    }
    bindSplitDirectionRadio('kioskCommodityDir', 'PromptKioskCommodityOnInbound',  'PromptKioskCommodityOnOutbound');
    bindSplitDirectionRadio('kioskCustomerDir',  'PromptKioskCustomerOnInbound',   'PromptKioskCustomerOnOutbound');
    bindSplitDirectionRadio('kioskLocationDir',  'PromptKioskLocationOnInbound',   'PromptKioskLocationOnOutbound');
    bindSplitDirectionRadio('kioskDestDir',      'PromptKioskDestinationOnInbound', 'PromptKioskDestinationOnOutbound');
    bindSplitDirectionRadio('kioskBinDir',       'PromptKioskBinOnInbound',         'PromptKioskBinOnOutbound');
    // Apply the rule on initial load (covers prompts already set to Neither).
    syncAllowSkipState();

    // --- Bin Inventory gating (System → Options) ---
    document.getElementById('BinRequired').addEventListener('change', syncAllowSkipState);

    document.getElementById('UseBinInventory').addEventListener('change', function () {
        var on = this.value === 'true';
        setSectionDisplay(document.getElementById('binManageSection'), on ? 'flex' : 'none');
        document.getElementById('binRequiredSection').style.display = on ? 'block' : 'none';
        document.getElementById('binLockSection').style.display = on ? 'block' : 'none';
        var orderRow = document.getElementById('binFieldOrderRow');
        if (orderRow) orderRow.style.display = on ? 'table-row' : 'none';
        var promptRow = document.getElementById('binPromptRow');
        if (promptRow) promptRow.style.display = on ? 'table-row' : 'none';
        // Turning the feature on with the prompt at Neither would look broken
        // at the kiosk — default the prompt to Inbound (the receiving leg).
        if (on) {
            var inRadio = document.getElementById('kioskBinDirInbound');
            var noneRadio = document.getElementById('kioskBinDirNone');
            if (inRadio && noneRadio && noneRadio.checked) {
                inRadio.checked = true;
                inRadio.dispatchEvent(new Event('change'));
            }
        }
    });

    // --- Sample-data picker: stamp the chosen sample on the hidden form and post ---
    var sampleLoadBtn = document.getElementById('sampleDataLoadBtn');
    if (sampleLoadBtn) {
        sampleLoadBtn.addEventListener('click', function () {
            var chosen = document.querySelector('input[name="sampleTypeRadio"]:checked');
            document.getElementById('sampleTypeField').value = chosen ? chosen.value : 'gravel';
            document.getElementById('loadTestDataForm').submit();
        });
    }

    // --- Locations editor (Setup → Locations) ---
    // Physical sites: name/address/phone rows that save on change via
    // /api/sites, independent of the settings form's auto-save.
    (function () {
        var sites = [];
        var siteStatusTimer = null;

        function escLoc(s) {
            return String(s ?? '').replace(/[&<>"']/g, function (c) {
                return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
            });
        }

        function siteStatus(msg, isError) {
            var el = document.getElementById('siteStatus');
            clearTimeout(siteStatusTimer);
            el.innerHTML = '<span class="' + (isError ? 'text-danger' : 'text-success') + '">' + escLoc(msg) + '</span>';
            if (!isError) siteStatusTimer = setTimeout(function () { el.innerHTML = '&nbsp;'; }, 2000);
        }

        function loadSites() {
            fetch('/api/sites').then(function (r) { return r.json(); }).then(function (list) {
                sites = list;
                renderSites();
            });
        }

        function renderSites() {
            var tbody = document.querySelector('#sitesTable tbody');
            var html = '';
            sites.forEach(function (s) {
                html += '<tr data-id="' + s.id + '">'
                    + '<td><input class="form-control form-control-sm st-name" maxlength="100" value="' + escLoc(s.name) + '"></td>'
                    + '<td><input class="form-control form-control-sm st-address" maxlength="200" value="' + escLoc(s.address || '') + '"></td>'
                    + '<td><input class="form-control form-control-sm st-city" maxlength="50" value="' + escLoc(s.city || '') + '"></td>'
                    + '<td><input class="form-control form-control-sm st-state" maxlength="20" value="' + escLoc(s.state || '') + '"></td>'
                    + '<td><input class="form-control form-control-sm st-zip" maxlength="20" value="' + escLoc(s.zip || '') + '"></td>'
                    + '<td><input class="form-control form-control-sm st-phone" maxlength="30" value="' + escLoc(s.phone || '') + '"></td>'
                    + '<td><input type="number" step="1" class="form-control form-control-sm st-order" value="' + s.sortOrder + '"></td>'
                    + '<td class="text-center"><input type="checkbox" class="form-check-input st-active"' + (s.active ? ' checked' : '') + '></td>'
                    + '<td class="text-end"><button type="button" class="btn btn-outline-danger btn-sm py-0 st-delete">Delete</button></td>'
                    + '</tr>';
            });
            tbody.innerHTML = html;
            tbody.querySelectorAll('input').forEach(function (el) {
                el.addEventListener('change', function () { saveSiteRow(el.closest('tr')); });
            });
            tbody.querySelectorAll('.st-delete').forEach(function (btn) {
                btn.addEventListener('click', function () { deleteSite(parseInt(btn.closest('tr').dataset.id)); });
            });
        }

        function saveSiteRow(tr) {
            var id = parseInt(tr.dataset.id);
            var body = {
                id: id,
                name: tr.querySelector('.st-name').value.trim(),
                address: tr.querySelector('.st-address').value.trim() || null,
                city: tr.querySelector('.st-city').value.trim() || null,
                state: tr.querySelector('.st-state').value.trim() || null,
                zip: tr.querySelector('.st-zip').value.trim() || null,
                phone: tr.querySelector('.st-phone').value.trim() || null,
                sortOrder: parseInt(tr.querySelector('.st-order').value) || 0,
                active: tr.querySelector('.st-active').checked
            };
            fetch('/api/sites/' + id, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(body)
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Save failed.'); });
                siteStatus('Saved ✓', false);
                loadSites();
            }).catch(function (e) {
                siteStatus(e.message, true);
                loadSites();
            });
        }

        document.getElementById('addLocationBtn').addEventListener('click', function () {
            var n = sites.length + 1;
            var name = 'Location ' + n;
            while (sites.some(function (s) { return s.name === name; })) { n++; name = 'Location ' + n; }
            var maxOrder = sites.reduce(function (m, s) { return Math.max(m, s.sortOrder); }, 0);
            fetch('/api/sites', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ name: name, sortOrder: maxOrder + 10, active: true })
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Add failed.'); });
                loadSites();
            }).catch(function (e) { siteStatus(e.message, true); });
        });

        function deleteSite(id) {
            if (!confirm('Delete this location? Its scales, bins, and commodities become available at every location.')) return;
            fetch('/api/sites/' + id, { method: 'DELETE' }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Delete failed.'); });
                loadSites();
            }).catch(function (e) { siteStatus(e.message, true); });
        }

        loadSites();
    })();

    // --- Email editor (Setup → Email) ---
    // SMTP settings, scheduled Excel reports, and per-load email rules. Saves
    // through /api/email/*, independent of the settings form's auto-save.
    (function () {
        var schedules = [];
        var rules = [];
        var options = { customers: [], commodities: [], sites: [] };
        var editingScheduleId = null;
        var editingRuleId = null;
        var statusTimers = {};

        var DAY_NAMES = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];

        function esc(s) {
            return String(s ?? '').replace(/[&<>"']/g, function (c) {
                return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
            });
        }

        function status(elId, msg, isError) {
            var el = document.getElementById(elId);
            clearTimeout(statusTimers[elId]);
            el.innerHTML = '<span class="' + (isError ? 'text-danger' : 'text-success') + '">' + esc(msg) + '</span>';
            if (!isError) statusTimers[elId] = setTimeout(function () { el.innerHTML = '&nbsp;'; }, 3000);
        }

        function fmtDate(iso) {
            if (!iso) return '—';
            return new Date(iso).toLocaleString([], {
                month: 'numeric', day: 'numeric', year: '2-digit', hour: 'numeric', minute: '2-digit'
            });
        }

        function fmtTime(hour, minute) {
            var d = new Date(2000, 0, 1, hour, minute);
            return d.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' });
        }

        function scheduleWhen(s) {
            var at = fmtTime(s.sendHour, s.sendMinute);
            if (s.frequency === 'Weekly') return 'Weekly · ' + DAY_NAMES[s.dayOfWeek] + ' ' + at;
            if (s.frequency === 'Monthly') return 'Monthly · day ' + s.dayOfMonth + ' ' + at;
            return 'Daily · ' + at;
        }

        // Compact filter summary for the list: names when there are one or two,
        // a count beyond that, so the column stays readable.
        function filterSummary(s) {
            var parts = [];
            function add(label, csv) {
                var names = (csv || '').split(',').map(function (v) { return v.trim(); })
                    .filter(function (v) { return v.length; });
                if (!names.length) return;
                parts.push(names.length <= 2 ? names.join(', ') : names.length + ' ' + label);
            }
            add('customers', s.customerFilter);
            add('commodities', s.commodityFilter);
            if (s.siteId) {
                var site = options.sites.filter(function (x) { return x.id === s.siteId; })[0];
                if (site) parts.push(site.name);
            }
            if (s.groupCommoditiesIntoSheets) parts.push('per-commodity sheets');
            return parts.length ? parts.join(' · ') : 'All loads';
        }

        function statusCell(lastError, lastOkIso) {
            if (lastError) return '<span class="text-danger" title="' + esc(lastError) + '">Failed</span>';
            if (lastOkIso) return '<span class="text-success">Sent ' + esc(fmtDate(lastOkIso)) + '</span>';
            return '<span class="text-muted">Not yet run</span>';
        }

        // ===== SMTP settings =====

        var settingsFields = ['emHost', 'emPort', 'emSecurity', 'emUsername', 'emPassword',
                              'emFromAddress', 'emFromName', 'emReplyTo'];

        // The stored password never comes back to the browser. The box stays
        // empty and only counts as a change once it's actually typed in, so an
        // ordinary save can't blank out a working password.
        var passwordTouched = false;
        document.getElementById('emPassword').addEventListener('input', function () {
            passwordTouched = true;
        });

        function loadSettings() {
            return fetch('/api/email/settings').then(function (r) { return r.json(); }).then(function (s) {
                document.getElementById('emEnabled').checked = s.enabled;
                document.getElementById('emHost').value = s.host || '';
                document.getElementById('emPort').value = s.port || 2525;
                document.getElementById('emSecurity').value = s.securityMode || 'StartTls';
                document.getElementById('emUsername').value = s.username || '';
                document.getElementById('emFromAddress').value = s.fromAddress || '';
                document.getElementById('emFromName').value = s.fromName || '';
                document.getElementById('emReplyTo').value = s.replyTo || '';

                var pw = document.getElementById('emPassword');
                pw.value = '';
                pw.placeholder = s.hasPassword ? 'Saved — type to replace' : '';
                passwordTouched = false;

                if (s.lastError) status('emSettingsStatus', 'Last send failed: ' + s.lastError, true);
            });
        }

        function saveSettings() {
            var body = {
                enabled: document.getElementById('emEnabled').checked,
                host: document.getElementById('emHost').value.trim(),
                port: parseInt(document.getElementById('emPort').value) || 0,
                securityMode: document.getElementById('emSecurity').value,
                username: document.getElementById('emUsername').value.trim(),
                password: document.getElementById('emPassword').value,
                changePassword: passwordTouched,
                fromAddress: document.getElementById('emFromAddress').value.trim(),
                fromName: document.getElementById('emFromName').value.trim(),
                replyTo: document.getElementById('emReplyTo').value.trim()
            };
            return fetch('/api/email/settings', {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(body)
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Save failed.'); });
                status('emSettingsStatus', 'Saved ✓', false);
                // Re-read so the password box resets to its "saved" placeholder.
                return loadSettings();
            }).catch(function (e) {
                status('emSettingsStatus', e.message, true);
            });
        }

        document.getElementById('emEnabled').addEventListener('change', saveSettings);
        settingsFields.forEach(function (id) {
            document.getElementById(id).addEventListener('change', saveSettings);
        });

        document.getElementById('emTestBtn').addEventListener('click', function () {
            var btn = this;
            var to = document.getElementById('emTestTo').value.trim();
            if (!to) { status('emSettingsStatus', 'Enter an address to send the test to.', true); return; }
            btn.disabled = true;
            status('emSettingsStatus', 'Sending test…', false);
            fetch('/api/email/test', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ to: to })
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Send failed.'); });
                status('emSettingsStatus', 'Test sent to ' + to + ' ✓', false);
            }).catch(function (e) {
                status('emSettingsStatus', e.message, true);
            }).finally(function () { btn.disabled = false; });
        });

        // ===== Filter pickers =====

        function loadOptions() {
            return fetch('/api/email/filter-options').then(function (r) { return r.json(); })
                .then(function (o) { options = o; });
        }

        function fillMulti(selectId, values) {
            var sel = document.getElementById(selectId);
            sel.innerHTML = values.map(function (v) {
                return '<option value="' + esc(v) + '">' + esc(v) + '</option>';
            }).join('');
        }

        function selectMulti(selectId, csv) {
            var wanted = (csv || '').split(',').map(function (v) { return v.trim().toLowerCase(); })
                .filter(function (v) { return v.length; });
            Array.prototype.forEach.call(document.getElementById(selectId).options, function (o) {
                o.selected = wanted.indexOf(o.value.toLowerCase()) >= 0;
            });
        }

        function readMulti(selectId) {
            var picked = Array.prototype.filter.call(document.getElementById(selectId).options,
                function (o) { return o.selected; }).map(function (o) { return o.value; });
            return picked.length ? picked.join(', ') : null;
        }

        // ===== Scheduled reports =====

        function loadSchedules() {
            return fetch('/api/email/schedules').then(function (r) { return r.json(); }).then(function (list) {
                schedules = list;
                renderSchedules();
            });
        }

        function renderSchedules() {
            var tbody = document.querySelector('#emSchedulesTable tbody');
            if (!schedules.length) {
                tbody.innerHTML = '<tr><td colspan="7" class="text-muted small py-2">'
                    + 'No scheduled reports yet. Add one to have Excel summaries emailed automatically.</td></tr>';
                return;
            }
            tbody.innerHTML = schedules.map(function (s) {
                var to = (s.recipients || '').split(',').map(function (v) { return v.trim(); })
                    .filter(function (v) { return v.length; });
                var toText = to.length <= 1 ? (to[0] || '—') : to[0] + ' +' + (to.length - 1);
                return '<tr data-id="' + s.id + '"' + (s.active ? '' : ' class="opacity-50"') + '>'
                    + '<td><strong>' + esc(s.name) + '</strong>' + (s.active ? '' : ' <span class="badge bg-secondary">off</span>') + '</td>'
                    + '<td class="small">' + esc(scheduleWhen(s)) + '</td>'
                    + '<td class="small" title="' + esc(s.recipients) + '">' + esc(toText) + '</td>'
                    + '<td class="small">' + esc(filterSummary(s)) + '</td>'
                    + '<td class="small">' + (s.active ? esc(fmtDate(s.nextRunUtc)) : '—') + '</td>'
                    + '<td class="small">' + statusCell(s.lastError, s.lastSuccessUtc) + '</td>'
                    + '<td class="text-end text-nowrap">'
                    + '<button type="button" class="btn btn-outline-secondary btn-sm py-0 ems-preview" title="Download the workbook without emailing it">Preview</button> '
                    + '<button type="button" class="btn btn-outline-primary btn-sm py-0 ems-run" title="Send now without changing the schedule">Send</button> '
                    + '<button type="button" class="btn btn-outline-secondary btn-sm py-0 ems-edit">Edit</button> '
                    + '<button type="button" class="btn btn-outline-danger btn-sm py-0 ems-delete">×</button>'
                    + '</td></tr>';
            }).join('');

            tbody.querySelectorAll('.ems-edit').forEach(function (b) {
                b.addEventListener('click', function () { openSchedule(rowId(b)); });
            });
            tbody.querySelectorAll('.ems-delete').forEach(function (b) {
                b.addEventListener('click', function () { deleteSchedule(rowId(b)); });
            });
            tbody.querySelectorAll('.ems-run').forEach(function (b) {
                b.addEventListener('click', function () { runSchedule(rowId(b), b); });
            });
            tbody.querySelectorAll('.ems-preview').forEach(function (b) {
                b.addEventListener('click', function () {
                    window.location = '/api/email/schedules/' + rowId(b) + '/preview';
                });
            });
        }

        function rowId(btn) { return parseInt(btn.closest('tr').dataset.id); }

        function syncFrequencyFields() {
            var freq = document.getElementById('emsFrequency').value;
            document.getElementById('emsDayOfWeekWrap').style.display = freq === 'Weekly' ? '' : 'none';
            document.getElementById('emsDayOfMonthWrap').style.display = freq === 'Monthly' ? '' : 'none';
            var hint = freq === 'Weekly'
                ? 'Covers the seven days ending the morning it runs.'
                : freq === 'Monthly'
                    ? 'Covers the previous calendar month.'
                    : 'Covers the previous day.';
            document.getElementById('emsPeriodHint').textContent = hint
                + ' Times are in the timezone set on the System tab.';
        }
        document.getElementById('emsFrequency').addEventListener('change', syncFrequencyFields);

        function openSchedule(id) {
            editingScheduleId = id;
            var s = schedules.filter(function (x) { return x.id === id; })[0] || {
                name: '', active: true, recipients: '', cc: '', frequency: 'Daily',
                sendHour: 6, sendMinute: 0, dayOfWeek: 1, dayOfMonth: 1,
                groupCommoditiesIntoSheets: false, includeGrossTare: true, sendWhenEmpty: false,
                customerFilter: '', commodityFilter: '', siteId: null, subjectTemplate: ''
            };

            document.getElementById('emScheduleModalTitle').textContent = id ? 'Edit Scheduled Report' : 'New Scheduled Report';
            document.getElementById('emScheduleModalError').textContent = '';
            document.getElementById('emsName').value = s.name;
            document.getElementById('emsActive').checked = s.active;
            document.getElementById('emsRecipients').value = s.recipients || '';
            document.getElementById('emsCc').value = s.cc || '';
            document.getElementById('emsFrequency').value = s.frequency;
            document.getElementById('emsDayOfWeek').value = s.dayOfWeek;
            document.getElementById('emsDayOfMonth').value = s.dayOfMonth;
            document.getElementById('emsTime').value =
                String(s.sendHour).padStart(2, '0') + ':' + String(s.sendMinute).padStart(2, '0');
            document.getElementById('emsGroupCommodities').checked = s.groupCommoditiesIntoSheets;
            document.getElementById('emsIncludeGrossTare').checked = s.includeGrossTare;
            document.getElementById('emsSendWhenEmpty').checked = s.sendWhenEmpty;
            document.getElementById('emsSubject').value = s.subjectTemplate || '';

            fillMulti('emsCustomers', options.customers);
            fillMulti('emsCommodities', options.commodities);
            selectMulti('emsCustomers', s.customerFilter);
            selectMulti('emsCommodities', s.commodityFilter);
            document.getElementById('emsSite').innerHTML =
                '<option value="">All locations</option>' + options.sites.map(function (x) {
                    return '<option value="' + x.id + '">' + esc(x.name) + '</option>';
                }).join('');
            document.getElementById('emsSite').value = s.siteId || '';

            syncFrequencyFields();
            new bootstrap.Modal(document.getElementById('emScheduleModal')).show();
        }

        document.getElementById('emAddScheduleBtn').addEventListener('click', function () { openSchedule(null); });

        document.getElementById('emScheduleSaveBtn').addEventListener('click', function () {
            var btn = this;
            var time = (document.getElementById('emsTime').value || '06:00').split(':');
            var body = {
                id: editingScheduleId || 0,
                name: document.getElementById('emsName').value.trim(),
                active: document.getElementById('emsActive').checked,
                recipients: document.getElementById('emsRecipients').value.trim(),
                cc: document.getElementById('emsCc').value.trim() || null,
                frequency: document.getElementById('emsFrequency').value,
                sendHour: parseInt(time[0]) || 0,
                sendMinute: parseInt(time[1]) || 0,
                dayOfWeek: parseInt(document.getElementById('emsDayOfWeek').value) || 0,
                dayOfMonth: parseInt(document.getElementById('emsDayOfMonth').value) || 1,
                groupCommoditiesIntoSheets: document.getElementById('emsGroupCommodities').checked,
                includeGrossTare: document.getElementById('emsIncludeGrossTare').checked,
                sendWhenEmpty: document.getElementById('emsSendWhenEmpty').checked,
                customerFilter: readMulti('emsCustomers'),
                commodityFilter: readMulti('emsCommodities'),
                siteId: parseInt(document.getElementById('emsSite').value) || null,
                subjectTemplate: document.getElementById('emsSubject').value.trim() || null
            };

            btn.disabled = true;
            var url = editingScheduleId ? '/api/email/schedules/' + editingScheduleId : '/api/email/schedules';
            fetch(url, {
                method: editingScheduleId ? 'PUT' : 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(body)
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Save failed.'); });
                bootstrap.Modal.getInstance(document.getElementById('emScheduleModal')).hide();
                status('emScheduleStatus', 'Saved ✓', false);
                return loadSchedules();
            }).catch(function (e) {
                document.getElementById('emScheduleModalError').textContent = e.message;
            }).finally(function () { btn.disabled = false; });
        });

        function deleteSchedule(id) {
            var s = schedules.filter(function (x) { return x.id === id; })[0];
            if (!confirm('Delete the scheduled report "' + (s ? s.name : '') + '"? It will stop sending.')) return;
            fetch('/api/email/schedules/' + id, { method: 'DELETE' }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Delete failed.'); });
                return loadSchedules();
            }).catch(function (e) { status('emScheduleStatus', e.message, true); });
        }

        function runSchedule(id, btn) {
            btn.disabled = true;
            status('emScheduleStatus', 'Sending…', false);
            fetch('/api/email/schedules/' + id + '/run', { method: 'POST' }).then(function (r) {
                return r.json().then(function (d) {
                    if (!r.ok) throw new Error(d.error || 'Send failed.');
                    status('emScheduleStatus', d.message || 'Sent ✓', !d.success);
                    return loadSchedules();
                });
            }).catch(function (e) {
                status('emScheduleStatus', e.message, true);
                loadSchedules();
            }).finally(function () { btn.disabled = false; });
        }

        // ===== Per-load rules =====

        function loadRules() {
            return fetch('/api/email/rules').then(function (r) { return r.json(); }).then(function (list) {
                rules = list;
                renderRules();
            });
        }

        function renderRules() {
            var tbody = document.querySelector('#emRulesTable tbody');
            if (!rules.length) {
                tbody.innerHTML = '<tr><td colspan="7" class="text-muted small py-2">'
                    + 'No load email rules yet. Add one to email a customer or broker as each load finishes.</td></tr>';
                return;
            }
            tbody.innerHTML = rules.map(function (r) {
                var to = (r.recipients || '').split(',').map(function (v) { return v.trim(); })
                    .filter(function (v) { return v.length; });
                var toText = to.length <= 1 ? (to[0] || '—') : to[0] + ' +' + (to.length - 1);
                return '<tr data-id="' + r.id + '"' + (r.active ? '' : ' class="opacity-50"') + '>'
                    + '<td><strong>' + esc(r.name) + '</strong>'
                    + (r.lastError ? ' <span class="text-danger" title="' + esc(r.lastError) + '">⚠</span>' : '') + '</td>'
                    + '<td class="small" title="' + esc(r.recipients) + '">' + esc(toText) + '</td>'
                    + '<td class="small">' + esc(r.customerFilter || 'Any') + '</td>'
                    + '<td class="small">' + esc(r.commodityFilter || 'Any') + '</td>'
                    + '<td class="small">' + esc(fmtDate(r.lastSentUtc)) + '</td>'
                    + '<td class="text-center"><input type="checkbox" class="form-check-input emr-active"' + (r.active ? ' checked' : '') + '></td>'
                    + '<td class="text-end text-nowrap">'
                    + '<button type="button" class="btn btn-outline-secondary btn-sm py-0 emr-edit">Edit</button> '
                    + '<button type="button" class="btn btn-outline-danger btn-sm py-0 emr-delete">×</button>'
                    + '</td></tr>';
            }).join('');

            tbody.querySelectorAll('.emr-edit').forEach(function (b) {
                b.addEventListener('click', function () { openRule(rowId(b)); });
            });
            tbody.querySelectorAll('.emr-delete').forEach(function (b) {
                b.addEventListener('click', function () { deleteRule(rowId(b)); });
            });
            tbody.querySelectorAll('.emr-active').forEach(function (cb) {
                cb.addEventListener('change', function () { toggleRule(rowId(cb), cb.checked); });
            });
        }

        function openRule(id) {
            editingRuleId = id;
            var r = rules.filter(function (x) { return x.id === id; })[0] || {
                name: '', active: true, recipients: '',
                customerFilter: '', commodityFilter: '', subjectTemplate: ''
            };

            document.getElementById('emRuleModalTitle').textContent = id ? 'Edit Load Email Rule' : 'New Load Email Rule';
            document.getElementById('emRuleModalError').textContent = '';
            document.getElementById('emrName').value = r.name;
            document.getElementById('emrActive').checked = r.active;
            document.getElementById('emrRecipients').value = r.recipients || '';
            document.getElementById('emrSubject').value = r.subjectTemplate || '';

            fillMulti('emrCustomers', options.customers);
            fillMulti('emrCommodities', options.commodities);
            selectMulti('emrCustomers', r.customerFilter);
            selectMulti('emrCommodities', r.commodityFilter);

            new bootstrap.Modal(document.getElementById('emRuleModal')).show();
        }

        document.getElementById('emAddRuleBtn').addEventListener('click', function () { openRule(null); });

        document.getElementById('emRuleSaveBtn').addEventListener('click', function () {
            var btn = this;
            var body = {
                id: editingRuleId || 0,
                name: document.getElementById('emrName').value.trim(),
                active: document.getElementById('emrActive').checked,
                recipients: document.getElementById('emrRecipients').value.trim(),
                customerFilter: readMulti('emrCustomers'),
                commodityFilter: readMulti('emrCommodities'),
                subjectTemplate: document.getElementById('emrSubject').value.trim() || null
            };

            btn.disabled = true;
            var url = editingRuleId ? '/api/email/rules/' + editingRuleId : '/api/email/rules';
            fetch(url, {
                method: editingRuleId ? 'PUT' : 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(body)
            }).then(function (r) {
                if (!r.ok) return r.json().then(function (e) { throw new Error(e.error || 'Save failed.'); });
                bootstrap.Modal.getInstance(document.getElementById('emRuleModal')).hide();
                status('emRuleStatus', 'Saved ✓', false);
                return loadRules();
            }).catch(function (e) {
                document.getElementById('emRuleModalError').textContent = e.message;
            }).finally(function () { btn.disabled = false; });
        });

        // The Active checkbox is inline in the list — a one-field PUT that
        // resends the rest of the rule unchanged.
        function toggleRule(id, active) {
            var r = rules.filter(function (x) { return x.id === id; })[0];
            if (!r) return;
            fetch('/api/email/rules/' + id, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    id: id, name: r.name, active: active, recipients: r.recipients,
                    customerFilter: r.customerFilter, commodityFilter: r.commodityFilter,
                    subjectTemplate: r.subjectTemplate
                })
            }).then(function (resp) {
                if (!resp.ok) return resp.json().then(function (e) { throw new Error(e.error || 'Save failed.'); });
                status('emRuleStatus', 'Saved ✓', false);
                return loadRules();
            }).catch(function (e) { status('emRuleStatus', e.message, true); loadRules(); });
        }

        function deleteRule(id) {
            var r = rules.filter(function (x) { return x.id === id; })[0];
            if (!confirm('Delete the rule "' + (r ? r.name : '') + '"? Loads will stop being emailed for it.')) return;
            fetch('/api/email/rules/' + id, { method: 'DELETE' }).then(function (resp) {
                if (!resp.ok) return resp.json().then(function (e) { throw new Error(e.error || 'Delete failed.'); });
                return loadRules();
            }).catch(function (e) { status('emRuleStatus', e.message, true); });
        }

        // Filter options first — the list renderers use site names from it.
        loadOptions().then(function () {
            loadSettings();
            loadSchedules();
            loadRules();
        });
    })();

    // --- Confirm modal ---
    var _confirmFormId = null;
    function showConfirmModal(title, message, formId, btnClass) {
        _confirmFormId = formId;
        document.getElementById('confirmModalTitle').textContent = 'Confirm Operation';
        document.getElementById('confirmModalBody').innerHTML = message.replace(/\n/g, '<br>');
        var btn = document.getElementById('confirmModalBtn');
        btn.className = 'btn btn-sm ' + btnClass;
        btn.textContent = title;
        new bootstrap.Modal(document.getElementById('confirmModal')).show();
    }
    function confirmModalSubmit() {
        if (_confirmFormId) {
            document.getElementById(_confirmFormId).submit();
        }
    }
</script>
}
