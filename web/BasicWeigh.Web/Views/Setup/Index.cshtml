@model BasicWeigh.Web.Models.AppSetup
@{
    ViewData["Title"] = "Setup";
}

<h3 class="mb-2">Setup</h3>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success alert-dismissible fade show py-1 mb-2" role="alert">
        @TempData["Message"]
        <button type="button" class="btn-close btn-sm" data-bs-dismiss="alert"></button>
    </div>
}

<div class="row g-3">
    <!-- Column 1 & 2: Settings Form -->
    <div class="col-md-8">
        <form asp-action="Index" method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <div class="row g-3">
                <!-- Column 1: Headers & Ticket Settings -->
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Company &amp; Ticket</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="mb-2">
                                <label asp-for="Header1" class="form-label mb-0 small"></label>
                                <input asp-for="Header1" class="form-control form-control-sm" />
                            </div>
                            <div class="mb-2">
                                <label asp-for="Header2" class="form-label mb-0 small"></label>
                                <input asp-for="Header2" class="form-control form-control-sm" />
                            </div>
                            <div class="mb-2">
                                <label asp-for="Header3" class="form-label mb-0 small"></label>
                                <input asp-for="Header3" class="form-control form-control-sm" />
                            </div>
                            <div class="mb-2">
                                <label asp-for="Header4" class="form-label mb-0 small"></label>
                                <input asp-for="Header4" class="form-control form-control-sm" />
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="TicketNumber" class="form-label mb-0 small"></label>
                                    <input asp-for="TicketNumber" class="form-control form-control-sm" type="number" />
                                </div>
                                <div class="col-6">
                                    <label asp-for="TicketsPerPage" class="form-label mb-0 small"></label>
                                    <input asp-for="TicketsPerPage" class="form-control form-control-sm" type="number" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Column 2: System Settings & Icon -->
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>System</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2 mb-2">
                                <div class="col-12">
                                    <label asp-for="DemoMode" class="form-label mb-0 small"></label>
                                    <select asp-for="DemoMode" class="form-select form-select-sm">
                                        <option value="false">Normal Mode</option>
                                        <option value="true">Demo Mode</option>
                                    </select>
                                </div>
                            </div>
                            <div class="row g-2 mb-2">
                                <div class="col-6">
                                    <label asp-for="KioskCount" class="form-label mb-0 small"></label>
                                    <select asp-for="KioskCount" class="form-select form-select-sm" id="KioskCount">
                                        <option value="0">No Kiosk</option>
                                        <option value="1">One Kiosk</option>
                                        <option value="2">In/Out Kiosk</option>
                                    </select>
                                </div>
                                <div class="col-6" id="kioskDarkModeSection" style="display: @(Model.KioskCount > 0 ? "block" : "none");">
                                    <label asp-for="KioskDarkMode" class="form-label mb-0 small"></label>
                                    <select asp-for="KioskDarkMode" class="form-select form-select-sm">
                                        <option value="true">Dark</option>
                                        <option value="false">Light</option>
                                    </select>
                                </div>
                            </div>
                            <div class="mb-2">
                                <label asp-for="Theme" class="form-label mb-0 small"></label>
                                <select asp-for="Theme" class="form-select form-select-sm">
                                    <option value="default">Default (Blue)</option>
                                    <option value="dark">Dark Mode</option>
                                    <option value="earth">Earth (Brown, Black &amp; Green)</option>
                                </select>
                            </div>
                            <div class="mb-2">
                                <label class="form-label mb-0 small">Icon</label>
                                <div class="d-flex align-items-center gap-2 mb-1">
                                    <img id="iconPreview" src="/api/setup/icon" alt="Current Icon" style="height: 36px; width: 36px;" />
                                    @if (Model.Icon != null)
                                    {
                                        <label class="form-check-label small">
                                            <input type="checkbox" name="removeIcon" id="removeIconCb" value="true" class="form-check-input" />
                                            Remove
                                        </label>
                                    }
                                    else
                                    {
                                        <span class="text-muted small">Default</span>
                                    }
                                </div>
                                <input type="file" name="iconFile" id="iconFileInput" class="form-control form-control-sm" accept="image/*" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row g-3 mt-1" id="kioskPromptsSection" style="display: @(Model.KioskCount > 0 ? "flex" : "none");">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                            <strong>Kiosk Prompts</strong>
                        </div>
                        <div class="card-body py-2">
                            <div class="row g-2">
                                <div class="col-md-4">
                                    <div class="form-check">
                                        <input asp-for="PromptKioskCarrier" class="form-check-input" id="PromptKioskCarrier" />
                                        <label asp-for="PromptKioskCarrier" class="form-check-label small"></label>
                                    </div>
                                </div>
                                <div class="col-md-4" id="truckIdPromptSection" style="display: @(Model.PromptKioskCarrier ? "block" : "none");">
                                    <div class="form-check">
                                        <input asp-for="PromptKioskTruckId" class="form-check-input" />
                                        <label asp-for="PromptKioskTruckId" class="form-check-label small"></label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check">
                                        <input asp-for="PromptKioskCommodity" class="form-check-input" />
                                        <label asp-for="PromptKioskCommodity" class="form-check-label small"></label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check">
                                        <input asp-for="PromptKioskCustomer" class="form-check-input" />
                                        <label asp-for="PromptKioskCustomer" class="form-check-label small"></label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check">
                                        <input asp-for="PromptKioskLocation" class="form-check-input" />
                                        <label asp-for="PromptKioskLocation" class="form-check-label small"></label>
                                    </div>
                                </div>
                                <div class="col-md-8">
                                    <label class="form-label mb-1 small fw-bold">Prompt Destination</label>
                                    <div class="d-flex gap-3">
                                        <div class="form-check">
                                            <input class="form-check-input" type="radio" name="kioskDestPrompt" id="destPromptNone" value="none"
                                                   @((!Model.PromptKioskDestinationOnInbound && !Model.PromptKioskDestinationOnOutbound) ? "checked" : "") />
                                            <label class="form-check-label small" for="destPromptNone">Neither</label>
                                        </div>
                                        <div class="form-check">
                                            <input class="form-check-input" type="radio" name="kioskDestPrompt" id="destPromptInbound" value="inbound"
                                                   @(Model.PromptKioskDestinationOnInbound ? "checked" : "") />
                                            <label class="form-check-label small" for="destPromptInbound">On Inbound</label>
                                        </div>
                                        <div class="form-check">
                                            <input class="form-check-input" type="radio" name="kioskDestPrompt" id="destPromptOutbound" value="outbound"
                                                   @(Model.PromptKioskDestinationOnOutbound ? "checked" : "") />
                                            <label class="form-check-label small" for="destPromptOutbound">On Outbound</label>
                                        </div>
                                    </div>
                                    <input type="hidden" asp-for="PromptKioskDestinationOnInbound" id="PromptKioskDestinationOnInbound" />
                                    <input type="hidden" asp-for="PromptKioskDestinationOnOutbound" id="PromptKioskDestinationOnOutbound" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-2">
                <button type="submit" class="btn btn-primary">Save Settings</button>
            </div>
        </form>
    </div>

    <!-- Column 3: Ticket Designers (outside the settings form) -->
    <div class="col-md-4">
        <div class="card">
            <div class="card-header py-1" style="background: var(--bw-surface-alt); color: var(--bw-text);">
                <strong>Ticket Designers</strong>
            </div>
            <div class="card-body py-2">
                <p class="small mb-2" style="color: var(--bw-text-muted);">Edit ticket layouts. Changes are saved and used when printing.</p>

                <div class="d-flex align-items-center gap-2 mb-2">
                    <a asp-controller="Ticket" asp-action="Designer" asp-route-reportName="TicketReport"
                       class="btn btn-outline-primary btn-sm d-flex align-items-center gap-1 flex-grow-1">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 6v.75m0 3v.75m0 3v.75m0 3V18m-9-5.25h5.25M7.5 15h3M3.375 5.25c-.621 0-1.125.504-1.125 1.125v3.026a2.999 2.999 0 010 5.198v3.026c0 .621.504 1.125 1.125 1.125h17.25c.621 0 1.125-.504 1.125-1.125v-3.026a2.999 2.999 0 010-5.198V6.375c0-.621-.504-1.125-1.125-1.125H3.375z" />
                        </svg>
                        <span class="fw-bold">Completed Ticket</span>
                    </a>
                    <form asp-action="ResetTicket" method="post" class="d-inline" onsubmit="return confirm('Reset Completed Ticket to default format? Any customizations will be lost.');">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="reportName" value="TicketReport" />
                        <button type="submit" class="btn btn-outline-danger btn-sm" title="Reset to default">Reset</button>
                    </form>
                </div>

                <div class="d-flex align-items-center gap-2 mb-2">
                    <a asp-controller="Ticket" asp-action="Designer" asp-route-reportName="KioskTicketReport"
                       class="btn btn-outline-primary btn-sm d-flex align-items-center gap-1 flex-grow-1">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 013.75 9.375v-4.5zM3.75 14.625c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5a1.125 1.125 0 01-1.125-1.125v-4.5zM13.5 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 0113.5 9.375v-4.5z" />
                            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 14.625v5.25h6v-5.25h-6z" />
                        </svg>
                        <span class="fw-bold">Kiosk Inbound Ticket</span>
                    </a>
                    <form asp-action="ResetTicket" method="post" class="d-inline" onsubmit="return confirm('Reset Kiosk Inbound Ticket to default format? Any customizations will be lost.');">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="reportName" value="KioskTicketReport" />
                        <button type="submit" class="btn btn-outline-danger btn-sm" title="Reset to default">Reset</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
    document.getElementById('iconFileInput').addEventListener('change', function (e) {
        var file = e.target.files[0];
        if (file) {
            var reader = new FileReader();
            reader.onload = function (ev) {
                document.getElementById('iconPreview').src = ev.target.result;
            };
            reader.readAsDataURL(file);
            // Uncheck remove if user picks a new file
            var cb = document.getElementById('removeIconCb');
            if (cb) cb.checked = false;
        }
    });

    document.getElementById('KioskCount').addEventListener('change', function () {
        var hasKiosk = parseInt(this.value) > 0;
        document.getElementById('kioskPromptsSection').style.display = hasKiosk ? 'flex' : 'none';
        document.getElementById('kioskDarkModeSection').style.display = hasKiosk ? 'block' : 'none';
    });

    document.getElementById('PromptKioskCarrier').addEventListener('change', function () {
        document.getElementById('truckIdPromptSection').style.display = this.checked ? 'block' : 'none';
    });

    document.querySelectorAll('input[name="kioskDestPrompt"]').forEach(function (radio) {
        radio.addEventListener('change', function () {
            document.getElementById('PromptKioskDestinationOnInbound').value = (this.value === 'inbound').toString().toLowerCase();
            document.getElementById('PromptKioskDestinationOnOutbound').value = (this.value === 'outbound').toString().toLowerCase();
        });
    });

    var removeCb = document.getElementById('removeIconCb');
    if (removeCb) {
        removeCb.addEventListener('change', function () {
            var preview = document.getElementById('iconPreview');
            if (this.checked) {
                preview.dataset.originalSrc = preview.src;
                preview.src = '/images/default-icon.svg';
            } else {
                preview.src = preview.dataset.originalSrc || '/api/setup/icon';
            }
        });
    }
</script>
}
