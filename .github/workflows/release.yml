name: Release

# Publishes prebuilt, self-contained builds so a site can be brought up without
# git, without the .NET SDK, and without waiting for a build on the target.
#
#   git tag v1.16.0 && git push origin v1.16.0
#
# Every target is cross-published from one Linux runner - self-contained publish
# needs no native toolchain for the target OS.
#
# The tarball layouts deliberately match what deploy/publish.sh and
# deploy/publish-pi-web.sh already produce, so the existing install.sh scripts
# work against a downloaded release unchanged.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build, without the leading v (e.g. 1.16.0)"
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Pinned, not "10.0.x", so a release is built with the SDK we chose rather
      # than whichever patch the runner resolved that morning. Bump it
      # deliberately, having built on it locally first.
      #
      # This is reproducibility, not a fix for anything: v1.23.0 failed and then
      # passed on re-run with no source change, both attempts on 10.0.401, so a
      # floating SDK was not the cause. See the note on the Razor flake below.
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.401"

      # DevExpress licensing is a build-time step, not a deployment one: a
      # source generator in DevExpress.Data reads this file and bakes the key
      # into Foundation.Web.dll, which is why the customer's PC needs no
      # licence. The runner has no key of its own, so without this step every
      # report the published app renders carries a trial watermark. Publishing
      # by hand from a dev machine works because the key is already there, at
      # %AppData%\DevExpress\DevExpress_License.txt on Windows.
      - name: Register the DevExpress licence
        env:
          DEVEXPRESS_LICENSE: ${{ secrets.DEVEXPRESS_LICENSE }}
        run: |
          if [ -z "$DEVEXPRESS_LICENSE" ]; then
            echo "::error::DEVEXPRESS_LICENSE secret is not set. Every artifact this workflow builds would ship with DevExpress trial watermarks on its reports."
            exit 1
          fi
          mkdir -p "$HOME/.config/DevExpress"
          printf '%s' "$DEVEXPRESS_LICENSE" > "$HOME/.config/DevExpress/DevExpress_License.txt"

      - name: Resolve version
        id: ver
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${GITHUB_REF_NAME#v}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building $VERSION"

      # ---- Foundation web app -------------------------------------------------
      #
      # Known flake: the win-x64 publish has failed once (v1.23.0) with a wall of
      # Razor errors — RZ1021, RZ1026, RZ2008 — across .cshtml files the commit
      # never touched, complaining about things that are not in the source: an
      # empty asp-for on a line that has a value, an unclosed <br> on a line that
      # reads <br />. It appeared 2.5s after restore, far too fast for a real
      # Razor compile of this app.
      #
      # It is the toolchain, not the code. The linux-x64 and linux-arm64 publishes
      # above had already compiled the same views successfully in the same run,
      # the same command succeeds locally, and a re-run of the same commit passed
      # with nothing changed — both attempts on SDK 10.0.401.
      #
      # If it recurs: re-run first, and do not go hunting in the .cshtml files it
      # names. Worth reporting upstream if it becomes frequent.
      #
      # linux-x64 for the Debian server, linux-arm64 for a LAN Pi. Layout mirrors
      # deploy/publish.sh: the app under ./foundation, with the unit file and
      # installer beside it.
      - name: Publish Foundation.Web (linux-x64)
        run: |
          dotnet publish web/Foundation.Web/Foundation.Web.csproj \
            -c Release -r linux-x64 --self-contained true \
            -o out/web-x64/foundation -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}
          cp deploy/foundation.service deploy/install.sh out/web-x64/

      - name: Publish Foundation.Web (linux-arm64)
        run: |
          dotnet publish web/Foundation.Web/Foundation.Web.csproj \
            -c Release -r linux-arm64 --self-contained true \
            -o out/web-arm64/foundation -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}
          cp deploy/foundation-pi.service out/web-arm64/
          cp deploy/install-pi-web.sh out/web-arm64/install.sh

      # win-x64 so a site that already runs Windows can install the server on a
      # PC it owns instead of adding a Linux box. Self-contained for the same
      # reason as everything else here, plus one specific to this app: the
      # DevExpress reporting packages need a licence key at build time, which a
      # customer does not have, so building on the target is not an option.
      - name: Publish Foundation.Web (win-x64)
        run: |
          dotnet publish web/Foundation.Web/Foundation.Web.csproj \
            -c Release -r win-x64 --self-contained true \
            -o out/web-win/app -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}
          cp deploy/install-web.ps1 deploy/INSTALL-WEB.bat out/web-win/
          cp deploy/package-README-web.txt out/web-win/README.txt

      # The licence is embedded, so it can also silently not be — an unset
      # secret is caught above, but an expired or malformed key fails soft and
      # the watermark only turns up once a customer prints a ticket. Assert the
      # generator actually wrote the attribute into the assembly.
      - name: Verify the licence was embedded
        run: |
          if ! grep -qa DXLicenseInformation out/web-win/app/Foundation.Web.dll; then
            echo "::error::No DevExpress licence attribute in the published Foundation.Web.dll. The key in DEVEXPRESS_LICENSE was rejected, so reports would carry trial watermarks."
            exit 1
          fi

      # ---- Kiosk print agent (Pi only) ---------------------------------------
      - name: Publish KioskPrintAgent (linux-arm64)
        run: |
          dotnet publish KioskPrintAgent/KioskPrintAgent.csproj \
            -c Release -r linux-arm64 --self-contained true \
            -o out/kiosk-arm64/app -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}

      # ---- RFID card reader service ------------------------------------------
      # arm64 for a Pi, win-x64 because a card reader is often on the weigh PC
      # itself. install.sh picks up ./app and skips its build, same as the
      # scale reader's release path.
      - name: Publish RfidReaderService (linux-arm64)
        run: |
          dotnet publish RfidReaderService/RfidReaderService.csproj \
            -c Release -r linux-arm64 --self-contained true \
            -o out/rfid-arm64/app -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}
          cp RfidReaderService/deploy/install.sh out/rfid-arm64/

      # The Windows package carries the installer, the double-clickable wrapper
      # and its own README, so a tech can unzip it on a customer PC and read
      # what to do without going back to GitHub - same layout as the scale
      # reader's win-x64 zip.
      - name: Publish RfidReaderService (win-x64)
        run: |
          dotnet publish RfidReaderService/RfidReaderService.csproj \
            -c Release -r win-x64 --self-contained true \
            -o out/rfid-win/app -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}
          cp RfidReaderService/deploy/install.ps1 RfidReaderService/deploy/INSTALL.bat out/rfid-win/
          cp RfidReaderService/deploy/package-README.txt out/rfid-win/README.txt

      # ---- Gate controller service (Pi only) ---------------------------------
      # arm64 only, deliberately: this service exists to drive GPIO, and there
      # is no GPIO header on a Windows scale house PC.
      - name: Publish GateControllerService (linux-arm64)
        run: |
          dotnet publish GateControllerService/GateControllerService.csproj \
            -c Release -r linux-arm64 --self-contained true \
            -o out/gate-arm64/app -p:PublishSingleFile=false \
            -p:Version=${{ steps.ver.outputs.version }}
          cp GateControllerService/deploy/install.sh out/gate-arm64/
          cp GateControllerService/deploy/package-README.txt out/gate-arm64/README.txt

      # ---- Raspberry Pi kiosk (shell only) -----------------------------------
      # Chromium watchdog scripts, not a compiled service, so there is nothing
      # to publish and no architecture to target - the package exists so a Pi
      # can be set up without git, a sparse checkout, or a GitHub account.
      - name: Stage RaspberryPiKiosk
        run: |
          mkdir -p out/kiosk-pi
          cp RaspberryPiKiosk/{install.sh,uninstall.sh,kiosk-loop.sh,kiosk-start,kiosk-stop,README.md} out/kiosk-pi/

      - name: Assemble packages
        run: |
          set -euo pipefail
          chmod +x out/web-x64/install.sh out/web-arm64/install.sh out/rfid-arm64/install.sh
          chmod +x out/gate-arm64/install.sh out/gate-arm64/app/GateControllerService
          chmod +x out/web-x64/foundation/Foundation.Web out/web-arm64/foundation/Foundation.Web
          chmod +x out/kiosk-arm64/app/KioskPrintAgent out/rfid-arm64/app/RfidReaderService
          chmod +x out/kiosk-pi/{install.sh,uninstall.sh,kiosk-loop.sh,kiosk-start,kiosk-stop}

          tar -czf foundation-web-linux-x64.tar.gz     -C out/web-x64 .
          tar -czf foundation-web-linux-arm64.tar.gz   -C out/web-arm64 .
          tar -czf kiosk-print-agent-linux-arm64.tar.gz -C out/kiosk-arm64 .
          tar -czf rfid-reader-linux-arm64.tar.gz      -C out/rfid-arm64 .
          tar -czf gate-controller-linux-arm64.tar.gz  -C out/gate-arm64 .
          tar -czf kiosk-pi.tar.gz                     -C out/kiosk-pi .
          (cd out/rfid-win && zip -qr ../../rfid-reader-win-x64.zip .)
          (cd out/web-win  && zip -qr ../../foundation-web-win-x64.zip .)

          ls -lh foundation-web-* kiosk-*.tar.gz rfid-* gate-*.tar.gz

      - name: Check the packages
        run: |
          set -euo pipefail
          # Verify what we think we built, rather than finding out on a customer's server.
          test -f out/web-x64/foundation/Foundation.Web     || { echo "x64 web binary missing";   exit 1; }
          test -f out/web-arm64/foundation/Foundation.Web   || { echo "arm64 web binary missing"; exit 1; }
          test -f out/web-x64/install.sh                    || { echo "x64 installer missing";    exit 1; }
          test -f out/web-arm64/install.sh                  || { echo "arm64 installer missing";  exit 1; }
          test -f out/kiosk-arm64/app/KioskPrintAgent       || { echo "kiosk binary missing";     exit 1; }
          test -f out/rfid-arm64/app/RfidReaderService      || { echo "rfid binary missing";      exit 1; }
          test -f out/rfid-win/app/RfidReaderService.exe    || { echo "rfid exe missing";         exit 1; }
          test -f out/gate-arm64/app/GateControllerService  || { echo "gate binary missing";      exit 1; }
          test -f out/gate-arm64/install.sh                 || { echo "gate installer missing";   exit 1; }
          test -f out/web-win/app/Foundation.Web.exe        || { echo "windows web exe missing";  exit 1; }

          # A package with no installer in it is worse than a failed build: it
          # looks complete right up to the moment someone unzips it on site.
          test -f out/rfid-win/install.ps1                  || { echo "rfid installer missing";   exit 1; }
          test -f out/rfid-win/INSTALL.bat                  || { echo "rfid wrapper missing";     exit 1; }
          test -f out/rfid-win/README.txt                   || { echo "rfid README missing";      exit 1; }
          test -f out/web-win/install-web.ps1               || { echo "windows web installer missing"; exit 1; }
          test -f out/web-win/INSTALL-WEB.bat               || { echo "windows web wrapper missing";   exit 1; }
          test -f out/web-win/README.txt                    || { echo "windows web README missing";    exit 1; }
          test -f out/gate-arm64/README.txt                 || { echo "gate README missing";      exit 1; }

          # The Pi kiosk package is scripts only - an installer shipped without
          # the watchdog beside it would wire an autostart entry to nothing.
          test -x out/kiosk-pi/install.sh                   || { echo "pi kiosk installer missing"; exit 1; }
          test -x out/kiosk-pi/kiosk-loop.sh                || { echo "pi kiosk watchdog missing";  exit 1; }
          test -x out/kiosk-pi/kiosk-start                  || { echo "pi kiosk start missing";     exit 1; }
          test -x out/kiosk-pi/kiosk-stop                   || { echo "pi kiosk stop missing";      exit 1; }
          bash -n out/kiosk-pi/install.sh                   || { echo "pi kiosk installer does not parse"; exit 1; }
          bash -n out/kiosk-pi/kiosk-loop.sh                || { echo "pi kiosk watchdog does not parse";  exit 1; }

          # A database must never ship - it would overwrite a site's own data.
          if find out -name "*.db" -o -name "*.db-wal" -o -name "*.db-shm" | grep -q .; then
            echo "::error::A database file leaked into a package."
            find out -name "*.db*"
            exit 1
          fi

          # Cross-published binaries must actually be for the target architecture.
          file out/web-arm64/foundation/Foundation.Web | grep -q "ARM aarch64" \
            || { echo "::error::arm64 web build is not an aarch64 binary"; exit 1; }
          file out/web-x64/foundation/Foundation.Web | grep -q "x86-64" \
            || { echo "::error::x64 web build is not an x86-64 binary"; exit 1; }
          file out/gate-arm64/app/GateControllerService | grep -q "ARM aarch64" \
            || { echo "::error::gate build is not an aarch64 binary"; exit 1; }
          file out/web-win/app/Foundation.Web.exe | grep -q "PE32+" \
            || { echo "::error::windows web build is not a PE32+ executable"; exit 1; }
          echo "Packages OK"

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          V: ${{ steps.ver.outputs.version }}
        run: |
          NOTES=$(cat <<EOF
          Prebuilt, self-contained builds. No .NET, no SDK and no git needed on
          the target machine.

          ## Foundation web app

          Debian server:

          \`\`\`
          curl -fsSL -o web.tar.gz https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/foundation-web-linux-x64.tar.gz
          mkdir -p /tmp/fw && tar -xzf web.tar.gz -C /tmp/fw
          sudo DOMAIN=your.domain.com EMAIL=you@example.com bash /tmp/fw/install.sh
          \`\`\`

          Raspberry Pi: same, with \`foundation-web-linux-arm64.tar.gz\`.

          Omit DOMAIN/EMAIL for a LAN install without Let's Encrypt. The database
          is preserved across reinstalls.

          Windows (Server, 10 or 11): download
          \`foundation-web-win-x64.zip\`, unzip it, then right-click
          **INSTALL-WEB.bat** and choose *Run as administrator* — or from an
          **admin** command prompt in that folder:

          \`\`\`
          INSTALL-WEB.bat
          \`\`\`

          Installs a Windows service that starts at boot and restarts itself if
          it crashes, opens the firewall port, and waits until the site actually
          answers before reporting success. It prints the network address to
          point kiosks and services at. Useful switches:

          - \`-Port 80\` — serve the site with no port in the address. IIS must
            be off; the installer says so if it is in the way.
          - \`-Port 8080\` — listen somewhere other than 5110.
          - \`-InstallDir D:\Foundation\` — install somewhere other than \`C:\Foundation\`.
          - \`-ResetDb\` — start from an empty database. Destroys every ticket
            and setting; a timestamped backup is taken first regardless.

          Re-run the same command to update: the database, the login keys and
          any ticket layouts edited in the Report Designer are kept. Back up
          \`C:\Foundation\Foundation.db\` — that one file is the site's data.

          ## Raspberry Pi kiosk

          Chromium in kiosk mode with a watchdog that restarts it when the
          server has been unreachable. Scripts only - no .NET, and nothing
          architecture-specific.

          \`\`\`
          curl -fsSL -o kiosk.tar.gz https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/kiosk-pi.tar.gz
          mkdir -p ~/foundation/RaspberryPiKiosk && tar -xzf kiosk.tar.gz -C ~/foundation/RaspberryPiKiosk
          ~/foundation/RaspberryPiKiosk/install.sh
          sudo reboot
          \`\`\`

          \`~/foundation/RaspberryPiKiosk\` is the same path a monorepo checkout
          produces, so a Pi ends up with one Foundation folder however it was
          set up. Releases before this one said \`~/foundation-kiosk\`; the
          installer offers to clear that folder away once the new location is
          running.

          Extract it somewhere permanent - the autostart entry points at
          wherever you unpack it, so \`/tmp\` leaves a kiosk that stops working
          at the next reboot.

          The installer asks only for the server URL (plus an optional PIN and
          language). The kiosk then asks **on its own screen**, the first time
          it loads, which scale it weighs on, which printer to use, and which
          card reader to listen to - the last two can each be skipped. To
          change any of it later, press and hold the logo on the kiosk screen
          for three seconds, or edit the kiosk under **Setup → Kiosks**.

          The Pi must be set to Desktop Autologin (\`sudo raspi-config\`), or
          the autostart entry never fires.

          ## Kiosk print agent (Pi)

          \`kiosk-print-agent-linux-arm64.tar.gz\`

          ## RFID card reader service

          Reads HID / prox cards from an RS-232 reader and reports each
          presentation to the web app. Usually a Pi beside a kiosk, but the
          reader is often wired to the weigh PC instead — hence both builds.

          ### Windows

          Download \`rfid-reader-win-x64.zip\`, unzip it, then from an
          **admin** command prompt in that folder:

          \`\`\`
          INSTALL.bat https://your-web-app-url -SerialPort COM3
          \`\`\`

          Installs the service with automatic startup, keeps the existing
          database, and applies the web app URL. Re-run the same command to
          update. \`-SerialPort\` is optional — leave it out and add the reader
          from the web app instead. Add \`-ResetDb\` to start from a clean
          database.

          On a PC that also runs the Web Print Service, add \`-Port 5231\`:
          both default to 5230, and the second one to start fails to bind.

          ### Raspberry Pi (64-bit, Pi 4/5)

          \`\`\`
          curl -fsSL -o rrs.tar.gz https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/rfid-reader-linux-arm64.tar.gz
          mkdir -p /tmp/rrs && tar -xzf rrs.tar.gz -C /tmp/rrs
          bash /tmp/rrs/install.sh https://your-web-app-url --service-id kiosk-1
          \`\`\`

          Then open the kiosk display: a kiosk that has not been set up
          yet asks on its own screen which scale, printer and card reader
          to use, and the reader you just installed is in that list. The
          roster of kiosks is on the web app under **Setup → Kiosks**.

          ## Gate controller service

          Drives a gate relay and/or a light when a ticket completes, releasing
          once the truck drives off the scale or a timeout expires.

          ### Raspberry Pi (64-bit, Pi 4/5)

          \`\`\`
          curl -fsSL -o gate.tar.gz https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/gate-controller-linux-arm64.tar.gz
          mkdir -p /tmp/gcs && tar -xzf gate.tar.gz -C /tmp/gcs
          bash /tmp/gcs/install.sh https://your-web-app-url --service-id north-gate
          \`\`\`

          Then add the gates wired to that Pi at \`http://<pi>:5240/\`, and set
          each Scale's **Gate** on the web app to \`<service-id>:<gate-id>\`.
          Prove the wiring before a truck is involved:

          \`\`\`
          curl -X POST http://<pi>:5240/api/gates/gate-1/test
          \`\`\`

          ### Windows

          There is no Windows build, and there will not be one. This service
          exists to drive relays on the GPIO header, and a scale house PC has
          no GPIO.

          ## Scale reader service

          Released separately, from its own repo:
          https://github.com/GTMichelli-Dev/scale-reader-service/releases/latest

          ---

          The URL you pass an installer must match the web app's real scheme
          and port — a wrong one leaves the service reconnecting forever. Every
          installer is safe to re-run: that is also how you update, and the
          database is preserved.

          The RFID and gate archives carry a \`README.txt\` covering options,
          ports and what to check when something is wrong.

          ## Pi git auth

          None of the above needs git on the target — that is the point of
          shipping prebuilt. A Pi that also has to \`git clone\` or \`git pull\`
          a **private** GTMichelli-Dev repo (\`pi-network-setup\`,
          \`camera-capture-service\`, \`qb-sync-service\`) needs a one-time
          bootstrap first:

          https://github.com/GTMichelli-Dev/pi-git-auth
          EOF
          )

          gh release create "v$V" \
            --title "v$V" \
            --notes "$NOTES" \
            foundation-web-linux-x64.tar.gz \
            foundation-web-linux-arm64.tar.gz \
            kiosk-print-agent-linux-arm64.tar.gz \
            rfid-reader-linux-arm64.tar.gz \
            rfid-reader-win-x64.zip \
            foundation-web-win-x64.zip \
            gate-controller-linux-arm64.tar.gz             kiosk-pi.tar.gz
